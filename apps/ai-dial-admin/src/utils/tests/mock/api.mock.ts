import { Token } from '@/src/models/auth';

export const TEST_URL = 'test/';
export const RESPONSE_MOCK = { success: true };
export const TOKEN_MOCK = {
  access_token: 'access_token_mock',
} as unknown as Token;
