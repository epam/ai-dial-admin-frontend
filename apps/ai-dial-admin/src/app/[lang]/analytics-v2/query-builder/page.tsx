// Analytics 2.0 — Query Builder. Intentionally renders nothing in this iteration;
// the menu entry point and route exist so later stories can build the UI here.
export default function Page() {
  return null;
}
