export const ROW_HEIGHT = 48;
export const GRID_COLUMNS_KEY = 'gridColumnsState';
export const STACKED_LINE_HEIGHT = 22;
export const STACKED_ROW_PADDING = 10;
// Matches CellContextMenu's min-w-[140px] plus a margin so the menu never overflows the viewport.
export const CONTEXT_MENU_ESTIMATED_WIDTH = 160;
// Covers CellContextMenu's two possible menu items plus its py-1 container padding and a margin.
export const CONTEXT_MENU_ESTIMATED_HEIGHT = 90;
