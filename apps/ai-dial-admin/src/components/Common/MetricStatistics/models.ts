/** Known `metric_score_name` statistic values used by Metric Scores / Metric Trends controls. */
export enum MetricStatistic {
  Avg = 'AVG',
  P90 = 'P90',
  P10 = 'P10',
  Max = 'MAX',
  Min = 'MIN',
  Med = 'MED',
  Count = 'COUNT',
}
