## 1. Types and data utilities

- [ ] 1.1 Create `src/components/Runs/Details/BottomDrawer/types.ts` — define `ComparisonSection`, `ComparisonRow`, `DrawerPanelState` interfaces, `ViewMode` type ('table' | 'pivot'), `DetailMode` type ('sidebar' | 'drawer'), and named constants: `DEFAULT_DRAWER_HEIGHT = 380`, `MIN_DRAWER_HEIGHT = 200`, `MAX_DRAWER_OFFSET = 100`, `COLLAPSED_HEIGHT = 34`, `RESIZE_STEP = 20`, `RESIZE_STEP_LARGE = 100`
- [ ] 1.2 Create `src/components/Runs/Details/BottomDrawer/utils.ts` — implement `buildComparisonSections()` that transforms one or two `AnalyticsResult` objects + field visibility/section order into `ComparisonSection[]` (union keys across results for extractedColumns and metric groups, null for missing fields); serialize `Record<string, unknown>` field values to `raw: string | null` per type: null/undefined→null, string→as-is, number/boolean→String(), object/array→JSON.stringify(value, null, 2); plus `formatFieldValue()`, `getDiffClass()` (with numeric normalization via `Number()` comparison and JSON normalization via parse/stringify), `valuesAreEqual()` helpers, and `countDiffs(sections: ComparisonSection[]): number` that counts visible fields with differing values for the toolbar diff badge
- [ ] 1.3 Add i18n keys to `src/constants/i18n.ts` under `RunsI18nKey` for: Analysis, Fields, Order, Table, Pivot, Pin, Unpin, Collapse, Close, SwitchToDrawer, SwitchToSidebar, Spotlight, RemoveSpotlight, NoFieldsVisible, ShowMore, SelectAll, DeselectAll, Search, Diffs, and field section names (Execution, TestCaseData, ExtractedColumns, RequestResponse); note: add new keys incrementally during component tasks if labels change
- [ ] 1.4 Add i18n values to `src/locales/en.ts` for all new keys (including "No fields visible. Use the Fields panel to show fields." for NoFieldsVisible)
- [ ] 1.5 Unit tests for `buildComparisonSections()` — test with single `AnalyticsResult`, two results (pinned + active), same result for both (dedup), null/missing values, metric sections with infos, field visibility filtering, section reorder, union of keys across results with null for missing fields; also test `valuesAreEqual()` with numeric normalization ("0.500" vs "0.5"), JSON normalization (different key order), null vs non-null; also test `countDiffs()` returns correct count of differing visible fields; file: `src/components/Runs/Details/BottomDrawer/tests/utils.spec.ts`

## 2. Hooks

- [ ] 2.1 Create `src/components/Runs/View/useDetailMode.ts` (co-located with `Analytics.tsx`) — manages `detailMode` ('sidebar' | 'drawer'), `selectedResultId`, `drawerOpen` (boolean signal), **sole owner** of `sidebar.showSidebar()`/`sidebar.closeSidebar()` calls within Analytics.tsx (Analytics.tsx must NOT call sidebar APIs directly), exposes `switchToDrawer()`, `switchToSidebar(resultId)`, `openDetail(resultId)`, `closeDetail()`; preserves mode preference across close/reopen; clicking the same active row toggles the detail view closed (matching existing sidebar toggle behavior); clicking the pinned row (not active) updates the active ID without closing; cleanup effect on unmount closes sidebar (`sidebar.closeSidebar()`) and sets `drawerOpen = false`; does NOT call `useDrawerPanel` or `useFieldSelector` directly — `AnalyticsBottomDrawer` watches `drawerOpen` and handles its own cleanup; `switchToDrawer()` sets `drawerOpen = true` and `pendingFocus = true` flag — the drawer uses a ref callback to focus the toolbar's first focusable element once mounted (deferred because the portal DOM isn't available synchronously)
- [ ] 2.2 Create `src/components/Runs/Details/BottomDrawer/useDrawerPanel.ts` — called in `Analytics.tsx` (not inside AnalyticsBottomDrawer) so Analytics.tsx can read `currentHeight` directly; manages drawer-specific UI: `isOpen`, `panelHeight` (default: `DEFAULT_DRAWER_HEIGHT`), `isCollapsed`, `viewMode`, `activeId`, `pinnedId`, `currentHeight` (computed: panelHeight when expanded, COLLAPSED_HEIGHT when collapsed, 0 when closed — used by Analytics.tsx to reduce grid container height); exposes `open(id)`, `close()`, `collapse()`, `expand()`, `pin()`, `unpin()`, `setView()`, `clearPinIfMissing(resultIds: string[])` (must also clear the cached pinned AnalyticsResult ref); state and methods passed down to AnalyticsBottomDrawer as props; handles dedup when active === pinned; adds `window` resize listener to clamp `panelHeight` to `window.innerHeight - MAX_DRAWER_OFFSET` on browser resize
- [ ] 2.3 Create `src/components/Runs/Details/BottomDrawer/useFieldSelector.ts` — manages `fieldVisibility`, `sectionOrder`, `sectionHidden`, `spotlightedFields` (triggered from Table view rows, NOT from the FieldSelector sidebar), `searchQuery` (for field filtering); uses `useReducer` for `fieldVisibility` and `sectionOrder` to ensure stable references for useMemo downstream; exposes `toggleField()`, `toggleSectionCollapse()`, `toggleSectionHidden()`, `selectAllInSection(sectionKey)`, `deselectAllInSection(sectionKey)`, `setSearchQuery(query)`, `reorderSections()`, `moveSectionByKeyboard(sectionKey, direction)`, `toggleSpotlight()`, `resetAll()`, `allFieldsHidden` (computed boolean for empty state), `filteredSections` (sections/fields matching search query)
- [ ] 2.4 Unit tests for all three hooks — test state transitions, mode switching with sidebar calls, same-row toggle closes detail, clicking pinned row updates active without closing, pinned===active dedup, clearPinIfMissing with valid/invalid IDs (verify ref is cleared), field visibility toggling, selectAll/deselectAll per section, search query filtering, section reorder, spotlight toggle, reset on close (verify AnalyticsBottomDrawer's effect calls both useDrawerPanel.close AND useFieldSelector.resetAll when drawerOpen transitions to false), cleanup on unmount, window resize clamping on useDrawerPanel, useFieldSelector uses stable references (useReducer); files: `src/components/Runs/View/tests/useDetailMode.spec.ts` and `src/components/Runs/Details/BottomDrawer/tests/hooks.spec.ts`

## 3. Detail mode integration in Analytics.tsx

- [ ] 3.1 Update `src/components/Runs/View/Analytics.tsx` — integrate `useDetailMode` hook; change `onRowClicked` to call `openDetail(resultId)` which routes to sidebar or drawer based on mode
- [ ] 3.2 In sidebar mode: call `sidebar.showSidebar(<RunMetricDetailPanel resultId={...} onClose={...} onSwitchMode={switchToDrawer} />, 'w-[750px]')`; in drawer mode: render `AnalyticsBottomDrawer` and do NOT call `sidebar.showSidebar()`
- [ ] 3.3 Reduce the grid container's available height by `useDrawerPanel.currentHeight` (computed value: panelHeight when expanded, 34px when collapsed, 0 when closed) via inline style `style={{ height: \`calc(100% - ${currentHeight}px)\` }}` — do NOT use `padding-bottom` as AG Grid's virtual scrolling calculates rows from its own container height, not parent padding; no ResizeObserver needed
- [ ] 3.3a Call `useDrawerPanel.clearPinIfMissing(resultIds)` whenever the grid `results` state updates — pass the current array of valid result IDs so the pinned case auto-clears if its ID no longer exists after a grid data refresh
- [ ] 3.4 Add optional `onSwitchMode?: () => void` prop to `RunMetricDetailPanel` (`src/components/Runs/Details/RunMetricDetailPanel.tsx`); when provided, render a switcher icon button (`IconLayoutBottombar`) in the header next to the close button
- [ ] 3.5 Update/extend existing Analytics tests to verify: row click opens sidebar in default mode, re-click same row closes sidebar (toggle), clicking pinned row in drawer mode updates active without closing, mode switch closes sidebar and opens drawer, mode switch from drawer closes drawer and opens sidebar, close preserves mode preference, grid container height reduced by drawer currentHeight, clearPinIfMissing called on grid refresh (and clears ref), unmount cleans up both sidebar and drawer, focus moves to drawer toolbar on switchToDrawer (via deferred ref callback); file: `src/components/Runs/View/tests/Analytics.spec.tsx` (extend existing or create)

## 4. AnalyticsBottomDrawer shell component

- [ ] 4.1 Create `src/components/Runs/Details/BottomDrawer/AnalyticsBottomDrawer.tsx` — portal-rendered (`createPortal` to `document.body`) fixed-bottom panel with `z-[35]` (below Header at z-40); receives `useDrawerPanel` state/methods as props from Analytics.tsx (hook is called in Analytics.tsx, not here); calls `useFieldSelector` internally; watches `useDetailMode.drawerOpen` — when it transitions to false, calls `useDrawerPanel.close()` AND `useFieldSelector.resetAll()` (AnalyticsBottomDrawer is the single owner of drawer cleanup, not useDetailMode); contains resize handle, `DrawerToolbar`, and body with `FieldSelector` + comparison area; uses ref callback on toolbar container to handle deferred focus on initial mount (for switchToDrawer focus management)
- [ ] 4.2 Implement data fetching: `useEffect` on `activeId`/`pinnedId` calling `getTestCaseRunResultDetails()` from `src/app/[lang]/runs/actions.ts`; parallel fetch with `Promise.all`; cache pinned result in `useRef` until unpin (avoid re-fetch on row clicks); **important**: clear the ref in all code paths that remove pinned case — `unpin()`, `close()`, AND `clearPinIfMissing()`; show pinned column data immediately while active column loads; handle loading state (`DialLoader`) and error state (null result → error message; pinned fetch failure → show warning + active-only view)
- [ ] 4.3 Implement resize handle: 6px-tall bar spanning full width, centered 32px-wide grip indicator (3 horizontal lines), `bg-layer-3` on hover, `cursor: ns-resize`, focus-visible ring; implement drag logic (mousedown/mousemove/mouseup on document, min `MIN_DRAWER_HEIGHT`, max viewport - `MAX_DRAWER_OFFSET`); disable `transition: height` during active drag (re-enable on mouseup) to avoid lag; add keyboard resize support (Arrow Up/Down ±`RESIZE_STEP`, Shift+Arrow ±`RESIZE_STEP_LARGE`) when handle is focused; handle must be focusable (`tabIndex={0}`, `role="separator"`, `aria-orientation="horizontal"`)
- [ ] 4.4 Implement collapse/expand toggle (hide body + resize bar, height → ~34px with 150ms ease transition; restore on second click with same transition; report current height to parent for grid height adjustment)
- [ ] 4.5 Implement Escape key handler with two-layer guard: (1) check `event.defaultPrevented` — if a child handler already called `preventDefault()`, skip drawer close; (2) check whether `document.activeElement` is inside the drawer portal — if focus is outside the drawer (e.g., AG Grid popup or external dropdown), skip drawer close; this is more robust than relying solely on `defaultPrevented` since some third-party libraries may not call it on their Escape handlers
- [ ] 4.6 Component test for `AnalyticsBottomDrawer` — test renders via portal, loading state shown during fetch, error state on null result, collapse/expand with transition, Escape closes (including defaultPrevented guard), browser resize clamps height; file: `src/components/Runs/Details/BottomDrawer/tests/AnalyticsBottomDrawer.spec.tsx`

## 5. DrawerToolbar component

- [ ] 5.1 Create `src/components/Runs/Details/BottomDrawer/DrawerToolbar.tsx` — renders: title ("Analysis"), case count, Pin/Unpin controls (badge with pinned case name + X), diff count badge ("N diffs" — shown only when pinned case exists, computed via `countDiffs()`), view toggle buttons (Table | Pivot), "Switch to Sidebar" icon button (`IconLayoutSidebarRight`), Collapse button, Close button
- [ ] 5.2 Component test for `DrawerToolbar` — test pin/unpin badge display, diff count badge shown/hidden based on pinned state, view toggle active state, switcher callback fires, button click callbacks fire; file: `src/components/Runs/Details/BottomDrawer/tests/DrawerToolbar.spec.tsx`

## 6. FieldSelector component

- [ ] 6.1 Create `src/components/Runs/Details/BottomDrawer/FieldSelector.tsx` — 180px left sidebar with Fields/Order tabs
- [ ] 6.2 Implement Fields tab: search input at top for filtering fields by name (case-insensitive, hides sections with no matches); collapsible section headers with arrow + count + "Select All / Deselect All" toggle button; per-field checkbox rows with monospace field names; all fields enabled by default
- [ ] 6.3 Implement Order tab: draggable section rows using existing `DraggableItem` component (NOT `DraggableList` — it's for editable string lists) with a lightweight custom list container using `useDrop`, scoped `DndProvider` + `HTML5Backend`, numbered positions, eye toggle for section visibility; add keyboard reorder via `onKeyDown` on a focusable wrapper around each `DraggableItem` (`tabIndex={0}`, `role="listitem"`) that intercepts Arrow Up/Down and calls `moveSectionByKeyboard` — keeps `DraggableItem` unmodified; note: keyboard focus target is the wrapper row, not the drag handle itself (DraggableItem has no keyboard handling)
- [ ] 6.4 Component test for `FieldSelector` — test tab switching, checkbox toggle updates parent callback, section collapse/expand, eye toggle hides section, search input filters fields and hides empty sections, Select All/Deselect All toggles all fields in section; file: `src/components/Runs/Details/BottomDrawer/tests/FieldSelector.spec.tsx`

## 7. ComparisonTableView component

- [ ] 7.1 Create `src/components/Runs/Details/BottomDrawer/ComparisonTableView.tsx` — renders comparison table from `ComparisonSection[]`: sticky header row with test case column headers (name, status, duration), section group rows with **click-to-collapse** headers (arrow indicator, toggles field row visibility inline — local collapsed state independent of Field Selector), field rows with values; render empty state ("No fields visible. Use the Fields panel to show fields.") when `allFieldsHidden` is true; long values (>500 chars) show first 200 chars as truncated preview with "Show more" link that expands inline to scrollable pre block (max-height 180px); expanded state is transient (resets on row switch or view toggle); apply `animate-fadeIn` (opacity 0→1, 100ms ease-in) on mount for smooth view switch crossfade
- [ ] 7.2 Implement diff highlighting: compare raw values between pinned and active columns using `valuesAreEqual()` (with numeric normalization and JSON normalization), apply amber (numeric, based on `ComparisonRow.isNumeric`) or teal (text) background tint via `getDiffClass()` Tailwind classes
- [ ] 7.3 Implement per-row spotlight button (distinct icon from pin 📌) to toggle focus strip inclusion
- [ ] 7.4 Create `src/components/Runs/Details/BottomDrawer/FocusStrip.tsx` — horizontal scrollable strip of spotlighted field cards above the table; each card shows field label, badge, and values per test case; close button to remove from spotlight
- [ ] 7.5 Component test for `ComparisonTableView` — test section grouping, click-to-collapse section headers, diff class application for numeric and text diffs, no diff when values equal, spotlight toggle, focus strip rendering, fadeIn animation class present on mount; file: `src/components/Runs/Details/BottomDrawer/tests/ComparisonTableView.spec.tsx`

## 8. ComparisonPivotView component

- [ ] 8.1 Create `src/components/Runs/Details/BottomDrawer/ComparisonPivotView.tsx` — transposed table: test cases as rows (sticky left column with name + status), fields as columns with section/field name headers (sticky top); spotlighted fields indicated by accent-colored top border on column header (`border-t-2 border-accent-primary`); scrollable both directions; diff highlighting on active row cells using same `getDiffClass()` logic as Table view (amber for numeric, teal for text diffs); apply `animate-fadeIn` (opacity 0→1, 100ms ease-in) on mount for smooth view switch crossfade
- [ ] 8.2 Component test for `ComparisonPivotView` — test row per test case, column per visible field, sticky positioning classes, single row when pinned === active, diff highlighting applied on active row cells when pinned case exists, spotlighted field column headers have accent border, fadeIn animation class present on mount; file: `src/components/Runs/Details/BottomDrawer/tests/ComparisonPivotView.spec.tsx`

## 9. Styling

Note: Styling is folded into each component task above (Tailwind utility classes applied directly in component JSX). No standalone styling task — all components use existing theme tokens (`bg-layer-*`, `text-secondary`, `border-primary`, `stroke-accent-primary`, etc.). The drawer portal uses `z-[35]`.

## 10. Quality checks

- [ ] 10.1 Run `npm run lint` and fix any lint errors
- [ ] 10.2 Run `npm run format:write` to apply formatting
- [ ] 10.3 Run `npm run test` and ensure all tests pass (existing + new)
