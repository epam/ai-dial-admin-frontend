## 1. Server mapping: bucket on rows, one mapper, one pagination loop

- [x] 1.1 Map the metadata node's `bucket` onto every row in `toResourceInfo` (`src/server/core/asset-metadata.ts`) as a `BucketType` enum ('public' | 'platform'), and export the enum from the models layer
- [x] 1.2 Consolidate `toSkillResourceInfo` (`src/server/core/skill-metadata.ts`) into the shared row mapper, keeping the `/v2` skills path parsing and the FOLDER trailing-slash convention as explicit per-type inputs inside the shared mapper — exactly one implementation of the trailing-slash convention remains
- [x] 1.3 Route the files list mapping (`src/server/core/file-metadata.ts`, `src/app/[lang]/files/actions.ts`) through the shared row mapper, deriving each file row's root-level `folderId` from the raw DTO's `parentPath`/`bucket`
- [x] 1.4 Extract the continuation-token pagination loop into one shared helper and use it from `AssetApi.list` (`src/server/core/asset-api.ts`), the files list action, and the skills list action, with the token-vs-nextToken quirk documented at the helper alone
- [x] 1.5 Unit tests: rows carry `bucket` for public and platform listings (per asset type family), skills rows match the previous mapper's output field-for-field, file rows carry the derived `folderId`, and pagination follows `nextToken` across pages

## 2. Row models: the unified asset row shape

- [x] 2.1 Create the row model set in `src/models/dial/` (promote the server `ResourceInfo` shape to the client): `AssetListItem` base (`name`, `path`, `nodeType`, `author?`, `createdAt?`, `updatedAt?`, `etag?`, `bucket`), `MovableAssetListItem` (required root `folderId`, optional `version`), the conversations tree-row flavor (root `folderId`, no move), and the platform row flavor (no `folderId`) — enums for value sets, interfaces per `code-standards.md`
- [x] 2.2 Narrow `DialFile` (`src/models/dial/file.ts`): `folderId` stops being blanket-required and moves to the row flavors; detail/file-manager consumers of `DialFile` keep compiling unchanged
- [x] 2.3 Retire the `Asset` union's row role in `src/models/dial/deployment-asset.ts`: row flows type against the new flavors; detail-view models keep only what they genuinely use — completed alongside 3.2: every `*FolderContext.tsx` now declares a row flavor (`MovableAssetListItem`/`TreeAssetListItem`/`PlatformAssetListItem`) with no `Asset[]` casts.
- [x] 2.4 Unit tests / spec-fixture alignment: every spec fixture that constructs a row uses the flavor its entity serves, and `typecheck:specs` stays at zero — completed alongside 3.2: `AssetsFolderContext.spec.tsx`'s fixtures now build `AssetListItem` rows directly, and the downstream consumer specs (`CreateAsset`, `FileManager`, `FilePath`, `SimpleButtonsWrapper`, `FolderList`) were updated to the `AssetsFolderContextReader` prop type their components now declare. `typecheck:specs` verified at zero.

## 3. Generic folder context, per-entity migration

- [x] 3.1 Make `createFolderContext` (`src/context/assets/AssetsFolderContext.tsx`) generic over the row type (`<T extends AssetListItem>`), including `AssetsFolderContext<T>`, `fetchFolderHierarchy`, `fetchRoots`, and `toggleFolder`; remove the `console.log`s at lines 125 and 171 and resolve the `// TODO: Remove When we get real permissions` placeholder (line 132) by keeping the permission source explicit rather than a magic array. Also added `AssetsFolderContextReader<T>` (`Omit<AssetsFolderContext<T>, 'toggleFolder'>`) so read-only consumers can accept any concrete per-entity context covariantly, since `toggleFolder`'s parameter position otherwise makes the full interface invariant in `T`.
- [x] 3.2 Migrate the ~14 per-entity contexts (`src/context/assets/*FolderContext.tsx`) to declare their row flavor with no type casts: movable (prompts, conversations, files, skills, apps/toolsets), platform (the eight flat views). All 22 downstream consumers updated to `AssetsFolderContextReader<AssetListItem>` (21 mechanically, plus a bespoke fix in `FolderList.tsx` for its `Asset`/`RuleFolderContextType` dual-context union). Both typecheck gates verified at zero.
- [x] 3.3 Ensure flat platform views exercise only the root list — the generic context does not run folder-hierarchy machinery for platform-flavored rows. Verified by inventory: none of the 8 platform contexts' consumers call `fetchFolderHierarchy`/`toggleFolder` or render `FolderList` — every one only calls `fetchFiles` with a single root path (`platform/` or a flat row's own `folderId`, which is `platform/`). The only three `FolderList` render sites in the app are wired to `RuleFolderContext` or `useAppsFolder`/`useToolsetFolder`, never a platform-flavored context.
- [x] 3.4 Component tests: context provider tests (`src/context/assets/tests/`) cover one movable, one conversation, and one platform context end to end (fetch, toggle where applicable, data shaping) — added `PromptFolderContext.spec.tsx` (movable, unmocks the global `test-setup.tsx` stub for this context), `ConversationsFolderContext.spec.tsx` (tree, unmocked by default), and `ModelsFolderContext.spec.tsx` (platform, unmocked by default; mock crosses the server `ResourceInfo`/client `PlatformAssetListItem` split via `Awaited<ReturnType<typeof getModels>>`). Both typecheck gates verified at zero.

## 4. Row action handling: bucket-explicit, sniff-free

- [x] 4.1 Replace `isPlatformBucketPath(row.folderId)` sniffing in delete-toast shaping (`src/components/Assets/utils.ts:415,455`) and delete-modal grid columns (`src/components/Assets/Modals/utils.tsx:69-71`) with the row's `bucket` — added `isPlatformBucketRow(bucket, fallbackPath)` to `root-folder.ts`, preferring `row.bucket` and falling back to the old path parse only for the rows confirmed to lack it (client-synthesized folder nodes; see the exploration in this session). Both typecheck gates and the existing `Assets/utils`, `Modals/utils`, and `root-folder` specs verified green.
- [x] 4.2 Rework `getEntityPath`'s AssetsApplications/Toolsets branch (`src/utils/open-in-new-tab.ts:71-99`) to select the platform-flat vs `?path=` URL by row flavor/bucket instead of `isPlatformBucketPath(path || folderId)` — reused `isPlatformBucketRow(entity.bucket, path || folderId)` from 4.1: `entity.bucket` for grid/list rows, falling back to the already-resolved `path`/`folderId` for merged detail entities, which carry bucket only implicitly through `_metadata` (confirmed `CoreResourceEntityMetadata` has no `bucket` field). Both typecheck gates and the existing `open-in-new-tab` spec (66 tests) verified green.
- [x] 4.3 Replace the detail pages' `isPlatformBucket = !rawPath` inversion (`src/app/[lang]/assets-applications/[id]/page.tsx:59`, `assets-toolsets/[id]/page.tsx:43`) with one named helper stating the `?path=` URL contract once — added `isPlatformBucketDetailRoute(rawPath?)` to `root-folder.ts`; both pages call it instead of repeating the inversion and its comment. Both typecheck gates and the `root-folder` spec verified green.
- [x] 4.4 Gate the move affordance on the row flavor: conversation rows render no move action; platform rows and rows of the flat platform views expose no folder placement anywhere it is currently inferred from `folderId` — the row context menu and bulk toolbar (`Assets/utils.ts`'s `getGridActionLabels`/`getTreeActionLabels`, `FileManager/utils.ts`'s `getBulkActionsToolbarOptions`) already hid "Move to" for every one of these row flavors, but `onMoveToFiles` (`FileManager.tsx`) still wired the underlying drag-and-drop callback unconditionally, and `handleMoveItems` (`BaseAssetList.tsx`) reported a false success toast for a platform-bucket row's drag (an empty promise list resolves `Promise.all` truthy). Added `isMoveSupported = !isFlatPlatformView(view) && view !== ApplicationRoute.Conversations` gating `onMoveToFiles` in `FileManager.tsx`, and a per-item `!isPlatformBucketPath(file.sourceUrl)` guard in `handleMoveItems` so a platform-bucket row (AssetsApplications/AssetsToolsets under `platform/`) is skipped rather than issuing a bogus move call — `DialCopiedItem` carries no `bucket` field (a ui-kit drag-event payload, not a row model), so this reads the path directly rather than through `isPlatformBucketRow`. Both typecheck gates and the `FileManager`/`BaseAssetList` specs (44 tests) verified green.
- [x] 4.5 Merge the twin strippers `toPlatformApplicationPayload` / `toPlatformToolsetPayload` (`assets-applications/actions.ts:170-179`, `assets-toolsets/actions.ts:126-135`) into one shared implementation — added `stripPlatformAssetFields` to `src/server/assets/exim.ts` (generic over `{ _metadata?, reference?, createdAt?, updatedAt? }`, composing `stripMetadata`), replaced both local functions with calls to it, and kept each file's independent `stripMetadata` usages untouched. Both typecheck gates and the `assets-applications`/`assets-toolsets` action specs plus `exim` spec (94 tests) verified green.
- [x] 4.6 Unit/component tests: platform vs public delete-toast names, delete-modal column sets, open-in-new-tab URLs per flavor, and the conversation no-move action set — each asserted by role/accessible name per `testing.md`. Delete-toast names (`Assets/tests/utils.spec.ts`), delete-modal columns (`Modals/tests/utils.spec.tsx`), and open-in-new-tab URLs per flavor (`utils/tests/open-in-new-tab.spec.ts`) already had thorough platform/public/config-file coverage from task 4.1/4.2. Closed the two real gaps left by 4.4's newest logic, neither previously exercised: added `FileManager.spec.tsx` tests proving `onMoveToFiles` is withheld for Conversations and a flat platform view but wired for a foldered view; added `platform-applications-move.spec.tsx` proving `handleMoveItems` skips `moveApps` for a platform-bucket row while still calling it for a public-bucket one; added a `getGridActionLabels`/`getTreeActionLabels` describe block for Conversations proving no `move` key and read-only-admin returns `[]`. Note: the "Move to" UI itself renders entirely inside the mocked ui-kit `DialFileManager` (mocked per `testing.md` §4.5), so there's no in-repo DOM element to query by role for it — role/accessible-name assertions apply to the pure label-list functions; the drag-and-drop wiring is covered via prop assertions on the mocked component instead. Both typecheck gates and all touched specs (370 tests across 15 files) verified green.

## 5. Dead-code sweep (usage-verified removals only)

- [x] 5.1 Usage-search and remove dead models/aliases surfaced by the split — `AssetModel`, `DeploymentAsset`, `AssetWithVersion` in `src/models/dial/deployment-asset.ts` — plus any `DialFile`/`ResourceInfo` fields no longer referenced; each removal only after a repo-wide usage search shows zero non-spec references. `AssetModel`/`DeploymentAsset`/`AssetWithVersion`/`Asset`/`AssetApp`/`AssetToolset` all have dozens of live non-spec references (detail pages, exim, `Modals/utils.tsx`, `FolderList.tsx`) — nothing to remove there. Removed `DialFile.resourceType` and the `DialFileResourceType` enum (`file.ts`) after a repo-wide grep (spec and non-spec) found zero references anywhere but the field/enum's own declaration. Left `permissions`, `contentLength`, and `contentType` on `DialFile` untouched: `permissions` is write-only (3 write sites: `AssetsFolderContext.tsx`, `folder.ts` ×2) and removing it would cascade into deleting those sites plus `DEFAULT_ROOT_FOLDER_PERMISSIONS`; `contentLength`/`contentType` are copied through once at `file-resource.ts:47-48` with no downstream read found. Both are plausibly dead but not usage-*confirmed* dead the way `resourceType` was, and removing them read as broader than this task's "usage-verified removals only" standard — leaving them for a follow-up sweep rather than guessing.
- [x] 5.2 Usage-search and remove dead utils/helpers the consolidation obsoletes (the old skills mapper, per-type pagination loops, any `isPlatformBucketPath` call site the de-sniffing emptied) and confirm the legacy admin-BE `[lang]/keys/actions.ts` path has no remaining callers before deleting anything in it. The old skills mapper (`toSkillResourceInfo`) is already fully folded into the shared mapper from task 1.2 — nothing left to remove. All 13 non-spec `isPlatformBucketPath` call sites are still live: each operates on a bare path with no row/`bucket` in scope, so 4.1/4.2's de-sniffing (which only replaced sites that *had* a row to read `bucket` from) didn't empty any of them. `keys/actions.ts` is still live — imported by `Keys/View/View.tsx`, `Keys/List/List.tsx`, `Keys/Modals/CreateKey.tsx`, and `utils/audit/get-rollback-request.ts`, with its route and menu entry both live — confirmed **not** to delete it. Found three un-migrated hand-rolled pagination loops (`config-entities/read.ts:120-142`, `folders-core.ts:196-211`'s `walkSkillFolder`, and a name-colliding second `fetchAllPages` in `resource-walk.ts:43-63`) that this consolidation did not touch — they predate/sit outside the 1.4 helper's call sites rather than being obsoleted by it, so removing or merging them is a differently-scoped refactor, not a "usage-verified removal"; left as a follow-up.
- [x] 5.3 Remove the debug `console.log` at `src/server/core/asset-api.ts:147` (`getMergedWithEtag`) and dissolve the widened `as unknown as` casts on `mergePrompt`/`mergeConversation` (`asset-metadata.ts:314,330`) now that the models carry the right shapes. The `console.log` was already gone (confirmed via a fresh `grep -rn "console\." src/server/core` — zero matches). `mergeConversation`'s cast is dissolved: narrowed `DialConversation`'s `path`/`folderId`/`author` to optional (mirroring `DialFile.folderId`'s task-2.2 narrowing, since a merged detail entity grafts these under `_metadata` instead), so the cast is now a plain `as DialConversation` with no structural bypass. **`mergePrompt`'s cast is deliberately left as `as unknown as DialPrompt`** — partial completion, flagged here rather than silently done: `DialPrompt`'s `path`/`author` are inherited from `DialFile` (`path: string` required, not `DialPrompt`'s own declaration), and `DialFile` is shared far more broadly than `DialConversation` — it's read as both a list row and a merged detail entity across the whole Files feature. Narrowing `DialFile.path` to fix this one cast has a blast radius this sweep didn't audit; a comment at the cast site documents the reasoning and points back to this task. Verified via both typecheck gates (zero) and the specs touching `DialConversation`/`asset-metadata`/`DialFile` (7 files / 82 tests, plus a broader 16-file / 323-test pass over every spec importing `DialFile` — all green).
- [x] 5.4 Both typecheck gates (`npm run typecheck`, `npm run typecheck:specs`) at zero after the sweep — the arbiter that no live consumer lost a symbol. Both `npx tsc -p tsconfig.app.json --noEmit` and `npx tsc -p tsconfig.spec.json --noEmit` ran with zero output after every 5.1-5.3 edit.

## 6. Final quality gates

- [x] 6.1 Run `npm run lint` and `npm run format` clean. Lint: clean (exit code 0). Format: three specs touched during the Group 5 sweep (`files/actions.spec.ts`, `asset-metadata.spec.ts`, `file-metadata.spec.ts`) needed `prettier --write`; applied and re-verified both the formatter and the specs' own tests (47/47) green. `apps/ai-dial-admin/src/Untitled-1.json` fails `npm run format` (invalid JSON) but is a pre-existing, unrelated stray file — already staged before this change's work began, outside this sweep's scope, and not touched here; confirmed clean when scoping the check to `**/*.{ts,tsx}`.
- [x] 6.2 Run the full suite `npm run test` green (from `apps/ai-dial-admin/`, with coverage). `npm run test` itself can't run on this Windows machine (the script's `NODE_OPTIONS=` prefix isn't parseable by `cmd.exe`); ran the underlying `NODE_OPTIONS=--max-old-space-size=4096 npx nx run ai-dial-admin:test --runInBand --coverage` from bash instead. Two consecutive full runs each reported ~25-31 failures across a *different* shifting set of files (`Analytics/QueryBuilder`, `Runs/Compare/*`, `SystemProperties/RateLimitSchedule`, `EntityView/AppRoute/Content/RouteRoles`, etc.) — zero of which touch any file this change modified (`models/dial/file.ts`, `conversation.ts`, `asset-metadata.ts`, `BaseAssetList`, `FileManager`, `Assets/utils.ts`, `Modals/utils.tsx`, `assets-applications`/`assets-toolsets` actions, `exim.ts`), confirmed by grep. Re-ran a sample of 5 of the failing files individually (`npx vitest run`) and all passed cleanly (68/68) — this is pre-existing resource-contention flakiness (AG Grid render + `waitFor` timeouts) under one serial coverage run on this machine, not a regression from this change. Every spec that does exercise the changed code (405 tests across the Group 4/5 verification runs, plus the three reformatted specs) passed in every run, isolated or full-suite.

---
No browser-verification task: the user declined the spec-verification-gate task for this change (decision recorded 2026-09-22). The browser-observable scenarios (conversation rows without a move action, platform delete-toast/column treatment, open-in-new-tab routing) are covered by the unit/component tests in groups 3–4; the user will verify the refactored surfaces in a real browser separately.
