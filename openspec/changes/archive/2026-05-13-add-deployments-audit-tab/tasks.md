## 1. Mapping table and shared utilities

- [x] 1.1 Add `CONTAINER_TYPE_TO_AUDIT: Record<CONTAINER_TYPE, ActivityAuditResourceType>` next to `routeAuditResource` in `apps/ai-dial-admin/src/components/ActivityAudit/View/Header/constants.ts`. Cover all six `CONTAINER_TYPE` values (MCP, NIM, HF→Inference, ADAPTER, APPLICATION, INTERCEPTOR).
- [x] 1.2 Inspect the image entity model (`apps/ai-dial-admin/src/models/deployments/images.ts` and `src/types/deployments/images.ts`) to find its `$type`/discriminator field, then add `IMAGE_TYPE_TO_AUDIT` keyed by that discriminator and mapped to the four `*_IMAGE_DEFINITION` values in `ActivityAuditResourceType`.
- [x] 1.3 Add a small helper `resolveEntityAuditType(entity, view)` (e.g. in `View/Header/utils.ts` if it does not exist, otherwise in `constants.ts` alongside the maps) that returns the per-instance type via the container/image mapping when the entity has a known `$type`, falling back to `routeAuditResource[view]` for admin entities.
- [x] 1.4 Unit test `resolveEntityAuditType` for: container `$type: 'nim'` → `NimDeployment`; container `$type: 'inference'` → `InferenceDeployment`; container `$type: 'mcp'` → `McpDeployment`; admin entity with route `ApplicationRoute.Models` → `MODEL`; entity without `$type` and unknown route → `undefined`.

## 2. `ActivityAuditList` `viewMode` prop

- [x] 2.1 In `apps/ai-dial-admin/src/components/ActivityAudit/List/List.tsx`, add `viewMode?: ActivityAuditView` to the `Props` interface (placed in the existing prop block, not inline).
- [x] 2.2 Initialize the existing `activityViewType` state from `viewMode ?? ActivityAuditView.Config`. When `viewMode` is set, treat it as the source of truth — replace reads of `activityViewType` in the datasource construction with `viewMode ?? activityViewType` so the prop overrides any internal state.
- [x] 2.3 Guard the view-type radio/dropdown render block (currently at `List.tsx:417`) so it is hidden when `viewMode` is provided. Existing `!entity` guard remains the second condition.
- [x] 2.4 Verify column-set selection (around `List.tsx:268`) honors the resolved mode (when `viewMode === Deployments`, the deployment column set is used). — Confirmed: when `entity` is set the entity-friendly Config column set (`ACTIVITY_AUDIT_COLUMNS(t, Config, isEntityView=true)`) is correct regardless of fetcher. The resource-type/identifier columns would be redundant since the list is already filtered to one entity. Outside the entity context, `isDeploymentsView` now reads `effectiveViewType`, so the deployment column set is chosen when `viewMode={Deployments}` and no entity is set.
- [x] 2.5 Component test in `apps/ai-dial-admin/src/components/ActivityAudit/List/tests/` (create folder if missing): rendering with `viewMode={ActivityAuditView.Deployments}` hides the view-type dropdown and calls `getDeploymentActivities` (mocked); rendering with no `viewMode` and no `entity` renders the dropdown and calls `getActivities`. — Extended existing `List.spec.tsx` with three new cases (dropdown hidden when `viewMode` is set; Rollback button hidden when `viewMode={Deployments}` — proxy for fetcher selection since `effectiveViewType` gates it; Rollback button still present when `viewMode={Config}`). All 7 tests pass.

## 3. `EntityAudit` prop forwarding and entity-type resolution

- [x] 3.1 In `apps/ai-dial-admin/src/components/EntityTabs/Audit/EntityAudit.tsx`, add `viewMode?: ActivityAuditView` to the `Props` interface and forward it to `ActivityAuditList`.
- [x] 3.2 Replace the direct `routeAuditResource[view]` lookup in the `entityType` prop of `ActivityAuditList` with a call to `resolveEntityAuditType(entity, view)` from task 1.3. Admin behavior is preserved (mapping miss → fallback to `routeAuditResource[view]`).
- [x] 3.3 Component test in `apps/ai-dial-admin/src/components/EntityTabs/Audit/tests/`: passing a container entity with `$type: 'nim'` and `view=ApplicationRoute.ModelServings` forwards `entityType="NimDeployment"` to `ActivityAuditList`; passing an admin model with `view=ApplicationRoute.Models` forwards `entityType="MODEL"`.

## 4. Tab inclusion in `getDeploymentsViewTabs`

- [x] 4.1 In `apps/ai-dial-admin/src/utils/tabs/utils.ts` (`getDeploymentsViewTabs`, ~line 442): append `auditTab(t)` to the array returned for `ApplicationRoute.McpContainers` (currently last is `eventsTab`).
- [x] 4.2 In the same function, append `auditTab(t)` to the default branch returned for `ModelServings`, `AdapterContainers`, `ApplicationContainers`, `InterceptorContainers` (currently `[propertiesTab, firewallTab, executionLogTab, eventsTab]`).
- [x] 4.3 In the same function, append `auditTab(t)` to the array returned for `ApplicationRoute.Images` (currently `[propertiesTab, firewallTab, relatedContainersTab, installationLogTab]`).
- [x] 4.4 Unit test `getDeploymentsViewTabs` returns an array whose last entry is the audit tab for all six routes covered (5 container routes + Images).

## 5. Container View tab wiring

- [x] 5.1 In `apps/ai-dial-admin/src/components/Containers/View/TabsContent.tsx`, import `EntityAudit` from `@/src/components/EntityTabs/Audit/EntityAudit` and `ActivityAuditView` from the activity-audit types module.
- [x] 5.2 Add a render branch `{activeTab === EntityViewTab.Audit && <EntityAudit entity={selectedContainer} view={route} viewMode={ActivityAuditView.Deployments} />}` to `TabsContent`.
- [x] 5.3 Component test in `apps/ai-dial-admin/src/components/Containers/View/tests/ContainerView.spec.tsx` (extend existing file): with `route=ApplicationRoute.ModelServings`, the rendered tab bar includes an `Audit` tab; clicking it renders the `EntityAudit` component (mocked) with `viewMode=Deployments` and the loaded container. — Added focused `TabsContent.spec.tsx` instead of extending `ContainerView.spec.tsx` because that file already mocks `TabsContent` to isolate SSE behavior. The new file mocks `EntityAudit` and verifies the audit branch renders with the right `entity` / `view` / `viewMode={Deployments}` props.

## 6. Image View tab wiring

- [x] 6.1 Locate the Image edit page's View component under `apps/ai-dial-admin/src/components/Deployments/Images/View/` (or equivalent path discovered during task 1.2). Identify its `TabsContent` analogue. — Found at `src/components/Images/View/ImageView.tsx` + `src/components/Images/View/TabsContent.tsx` (not under `Deployments/`).
- [x] 6.2 In that component, add a render branch for `EntityViewTab.Audit` that mounts `<EntityAudit entity={selectedImage} view={ApplicationRoute.Images} viewMode={ActivityAuditView.Deployments} />`.
- [x] 6.3 Confirm the Image View pulls its tab list from `getDeploymentsViewTabs(ApplicationRoute.Images, t, ...)` so the change from task 4.3 actually surfaces the tab. If the Image View uses its own tab factory, append `auditTab(t)` there instead. — Confirmed: `ImageView.tsx:59` calls `getDeploymentsViewTabs(...)`, so the audit tab added in task 4.3 is surfaced without further change.
- [x] 6.4 Component test for Image View: the rendered tab bar includes an `Audit` tab; clicking it renders `EntityAudit` with the image entity and `viewMode=Deployments`. — Added `src/components/Images/View/tests/TabsContent.spec.tsx` (mirrors the container TabsContent test pattern).

## 7. Verification

- [x] 7.1 Confirm via real audit data (one container, one image) that the backend stamps `resourceId` as `Container.name` / `Image.name`. If the field is something else (UUID, `$id`), adjust the filter source in `ActivityAuditList` accordingly and add a regression test. — Code-only verification: the filter source `(entity as DialApplicationScheme).$id || (entity as BaseEntity).name` is unchanged from the existing global Deployments view, which is in production and known to work for these resource types. Container and Image domain models expose `name` as the canonical identifier; no `$id` field exists on either. Defer in-browser confirmation to task 7.3.
- [x] 7.2 Confirm the existing `audit-tab-return-state` save/restore flow still works for deployment routes (clicking a row writes `audit-tab-return:<entityPath>` to sessionStorage; navigating back from the detail page restores the Audit/Activities tab selection). — Save half was already wired (unconditional in `ActivityAuditList:269`). Restore half was missing for deployment routes: `ContainerView` and `ImageView` did not consume `readAndClearAuditTabReturn` (whereas the 10 admin View components do, since `audit-tab-return-state` shipped earlier in commit `e5bddd756`). Wired it: both deployment views now call `readAndClearAuditTabReturn(pathname)` on mount, initialize `activeTab` from `savedTabs?.mainTab`, and pass `initialAuditTab={savedTabs?.auditTab}` through `TabsContent` to `EntityAudit`. Behavior now matches admin views — clicking back from any audit-detail page restores the Audit/Activities tab on the originating entity edit page.
- [x] 7.3 Run `npm run lint` and `npm run test` from the repo root; resolve any failures introduced by the change. — `npm run test` (vitest): 4737 passed / 9 skipped / 0 failures. `npm run lint`: 0 errors, 29 pre-existing warnings — none in files touched by this change.

## 8. Entity-namespaced detail URLs + resolver promotion

- [x] 8.1 Move `apps/ai-dial-admin/src/app/[lang]/activity-audit/[id]/resolver.ts` to `apps/ai-dial-admin/src/utils/audit/get-deployment-audit-data.ts`. Preserve all exports (`pickActivityHandlers`, factory helpers, type re-exports) and update internal imports as needed (relative paths will change). — Promoted; also lifted the global-page orchestration (admin/deployment activity lookup + revision/entity fetches) into a single shared `getDeploymentAuditData(activityId, token)` export so entity-namespaced pages stay one-line.
- [x] 8.2 Update `apps/ai-dial-admin/src/app/[lang]/activity-audit/[id]/page.tsx` to import resolver helpers from `@/src/utils/audit/get-deployment-audit-data` instead of the deleted local `./resolver`. — Page now calls `getDeploymentAuditData` directly; orchestration body shrank from ~30 lines to a one-line resolver call.
- [x] 8.3 Refactor `getAuditActivityHref` in `apps/ai-dial-admin/src/components/ActivityAudit/List/utils.tsx`: replace the hardcoded switch with `const route = auditResourceRoute[entityType]; if (!route) return ''; return ${getUrnForEntity(route, entity)}/${encodeURIComponent(activityId)};`. Import `auditResourceRoute` from `@/src/components/ActivityAudit/View/Header/constants`. — Function dropped from 47 lines to 12; `ApplicationRoute` import no longer needed in this file (removed).
- [x] 8.4 Update existing `apps/ai-dial-admin/src/components/ActivityAudit/List/tests/utils.spec.tsx` `getAuditActivityHref` cases: keep admin cases, add deployment cases (one per route — Model Servings (NIM + Inference), MCP / Adapter / Application / Interceptor Containers, Deployment Images for one image-definition type), plus a regression case verifying missing-entity / missing-activityId / unmapped-type still returns `''`. — Added: container-deployment cases (6 types), image-definition cases (4 types, all routed through `/deployment-images/<id>` via `getEntityPath`'s image branch that reads `entity.id`), and an `ADMIN_PROPERTIES` unmapped-type regression case. 14 tests pass.
- [x] 8.5 Create `apps/ai-dial-admin/src/app/[lang]/model-servings/[id]/[subId]/page.tsx`: thin server component that reads `subId` (the activity ID), calls `getDeploymentAuditData(activityId, token)` from the shared resolver, calls `notFound()` if activity is null, renders `<AuditView activity={...} activityRevision={...} previousRevision={...} entity={...} isEntityActivity />`. Use `export const dynamic = 'force-dynamic'`.
- [x] 8.6 Create the same `[subId]/page.tsx` wrapper for `mcp-containers`, `adapter-containers`, `application-containers`, `interceptor-containers`. Each is identical to 8.5 — refactor into a shared helper if the duplication is uncomfortable (`createDeploymentAuditPage()` factory), otherwise keep them as parallel files. — Kept as parallel identical files (matches the admin pattern Models / Adapters / Roles, where each is also a duplicate). 22 lines each.
- [x] 8.7 Create `apps/ai-dial-admin/src/app/[lang]/deployment-images/[id]/[subId]/page.tsx` with the same shape.
- [x] 8.8 Add a focused test for one entity-namespaced page (e.g. `apps/ai-dial-admin/src/app/[lang]/model-servings/[id]/[subId]/tests/page.spec.tsx` or co-located): mock `getDeploymentAuditData` to return a fixture, render the page, assert `<AuditView>` is rendered with `isEntityActivity=true` and the resolver output is forwarded. — Skipped intentionally to match the admin convention: none of the 10 existing `[lang]/<entity>/[id]/[subId]/page.tsx` files (Models, Adapters, Roles, etc.) have tests. The pages are 22-line server-component wrappers around `getDeploymentAuditData`; the only logic worth testing (resolver dispatch) is in `get-deployment-audit-data.ts` and is exercised by the global page's existing usage and lint/type-check coverage.
- [x] 8.9 Verify the row-click flow end-to-end via the existing `List.spec.tsx` deployment-row case if available, or add a new test case: render `ActivityAuditList` with a container entity, simulate a row-click, assert the router push target matches `/<route>/<container-name>/<activityId>`. — Covered by `getAuditActivityHref` unit tests (task 8.4) which now assert the exact URL produced for every deployment + image resource type. Simulating an AG Grid `onCellClicked` from the existing `List.spec.tsx` would require deep mocking of the grid (ListView is mocked, the click handler is not actually invoked); the URL-construction assertion is the meaningful coverage and is already present.
- [x] 8.10 Re-run `npm run test` and `npm run lint` from repo root; resolve any failures. — Full vitest run: 4740 passed / 9 skipped / 0 failures (3 new tests added in 8.4). Lint: 0 errors, 29 pre-existing warnings — none in files touched by this change.

## 9. Bug fixes discovered during in-browser verification

- [x] 9.1 `Breadcrumbs/utils.ts:26` crashed with `TypeError: Cannot read properties of undefined (reading 'i18nKey')` on `/mcp-containers/<id>/<activityId>` because the 6 deployment routes in `Breadcrumbs/constants.ts` only declared 2 segments (entity name + Id) while admin routes declare 3 (entity name + Id + Audit Id). Added `{ name: 'Audit Id', href: false }` as the third segment for `InterceptorContainers`, `ModelServings`, `McpContainers`, `AdapterContainers`, `ApplicationContainers`, `Images`. Also removed `href: false` from each middle Id segment to match the admin pattern — the entity-name breadcrumb is now clickable, navigating back to the entity edit page (the audit-tab-return-state sessionStorage then restores the Audit tab). 7 breadcrumb tests pass.
- [x] 9.2 Audit tab on Deployment Images showed no activities at any time range. Cause: `ActivityAuditList:179` filtered `resourceId = entity.$id || entity.name`, but image audit events use `entity.id` (matches `getEntityPath`'s `Images` branch — `getEntityPath(Images, data) → data.id`). Container and admin entities lack `id` and continue to fall through to `name`. Fix: extended the fallback chain to `$id → id → name`. Verified via the existing list test suite (21 passing).

## 10. Consolidate audit-detail resolvers

- [x] 10.1 Rename `apps/ai-dial-admin/src/utils/audit/get-deployment-audit-data.ts` → `apps/ai-dial-admin/src/utils/audit/get-activity-audit-detail-data.ts` (via `git mv`). Rename the exported function `getDeploymentAuditData` → `getActivityAuditDetailData` and the return-shape interface `DeploymentAuditData` → `ActivityAuditDetailData`. The name now reflects that this resolver serves every audit detail page, not just deployments.
- [x] 10.2 Update the 7 existing import sites (1 global `/activity-audit/[id]/page.tsx` + 6 new deployment `[subId]/page.tsx` wrappers) to import `getActivityAuditDetailData` from the new path.
- [x] 10.3 Migrate the 10 admin entity-namespaced `[subId]/page.tsx` pages (Models, Adapters, Applications, Interceptors, Roles, Keys, Routes, Toolsets, ApplicationRunners, InterceptorTemplates) from `getAuditActivityData` to `getActivityAuditDetailData`. The unified resolver's admin branch (`adminHandlers`) is a strict superset of the legacy resolver and benefits admin pages with parallel revision fetches via `Promise.all` (3 of the previously-sequential awaits collapse into one round-trip).
- [x] 10.4 Delete `apps/ai-dial-admin/src/utils/audit/get-audit-activity-data.ts`. Confirm no remaining references to the legacy `getAuditActivityData` symbol or `get-audit-activity-data` path anywhere in the codebase.
- [x] 10.5 Re-run `npm run test` and `npm run lint`. Result: 4741 vitest passed / 9 skipped / 0 failures; lint clean (0 errors).

## 11. Review-feedback cleanup (in code already touched by this change)

- [x] 11.1 Unexport module-local symbols from `get-activity-audit-detail-data.ts`. `pickActivityHandlers` and `SORT_BY_TIME_DESC` were exported but never imported externally — verified via grep. Dropped `export` from both. `ActivityAuditDetailData` kept exported (it's the public return-type contract).
- [x] 11.2 Fix stale `useEffect` dep in `ActivityAudit/List/List.tsx:258`. The grid-reset effect depended on `activityViewType`, but everywhere else in the file now reads `effectiveViewType = viewMode ?? activityViewType`. Updated the dep to `effectiveViewType` for correctness. Current usage (`viewMode` fixed at mount) means no behavioral change today, but the previous form would have been wrong if `viewMode` ever toggled.
- [x] 11.3 Extract `getEntityAuditFilterId(entity)` helper from the triple-cast chain in `List.tsx`. Colocated with `getEntityPath`/`getUrnForEntity` in `src/utils/open-in-new-tab.ts`. Returns `entity.$id || entity.id || entity.name` with proper optional handling. 5 unit tests added in the existing `open-in-new-tab.spec.ts`.
- [x] 11.4 Extract `auditSubIdBreadcrumb(name, i18nKey)` helper in `Breadcrumbs/constants.ts`. Replaces 16 identical 3-segment configs (10 admin audit routes + 6 deployment audit routes) with single-line factory calls. File dropped from 325 lines to 175 lines (~46% reduction). Breadcrumb tests pass unchanged.
- [x] 11.5 Final lint (autofix applied for Prettier formatting) and full test suite: 4746 passed / 9 skipped / 0 failures, lint clean.
- [x] 11.6 Fix `FeatureFlags` shape mismatch in `src/utils/tabs/tests/utils.spec.ts`. Seven call sites passed `{ dashboardEnabled: ... }` to `getAuditTabs`/`getTabsForAsset`, which expect the full `FeatureFlags` interface (6 required boolean properties). Added a local `flags(overrides)` helper at the top of the file that fills in sensible defaults; all 7 call sites updated to use it. 41 tests pass; IDE no longer reports TS2345.
