## 1. Preselect helper

- [x] 1.1 Create `apps/ai-dial-admin/src/utils/audit-list-preselect.ts` exporting `saveAuditListPreselect(value: 'global-firewall')`, `readAuditListPreselect(): 'global-firewall' | null` (pure read, no side effects), and `clearAuditListPreselect(): void` (explicit clear). The split keeps the consumer StrictMode-safe — the lazy initializer can call `readAuditListPreselect` idempotently, and the gridApi-ready effect calls `clearAuditListPreselect` only after the filter has been applied. Use the string-literal union type so the value set is checkable at compile time. Mirror the SSR guard (`typeof window === 'undefined'`) used in `audit-tab-return.ts`.
- [x] 1.2 Add unit tests in `apps/ai-dial-admin/src/utils/tests/audit-list-preselect.spec.ts` covering: write-then-read round-trip, read clears the key, read with no value returns null, read with invalid value returns null, SSR safety (no throw when localStorage is unavailable).

## 2. Resource-type filter reverse-map

- [x] 2.1 In `apps/ai-dial-admin/src/components/ActivityAudit/EntityGrid/utils.ts` (or wherever `getFormattedResourceType` lives — locate via grep), add an exported helper `buildResourceTypeLabelMap(t: TFn): Record<string, ActivityAuditResourceType[]>` that iterates `Object.values(ActivityAuditResourceType)` and groups raw enum values by the lowercased output of `getFormattedResourceType(value, t)`. Memoization is the caller's responsibility (we'll `useMemo` in `List.tsx`). _(Placed in `src/constants/grid-columns/formatters.ts` next to `getFormattedResourceType`.)_
- [x] 2.2 In `apps/ai-dial-admin/src/components/ActivityAudit/List/utils.tsx`, update `getGridFilters` to accept an optional `resourceTypeLabelMap` parameter. When a filter with `column === 'resourceType'` and AG Grid filter type `contains` is present, intercept it: lowercase the input, scan the map's keys for substring matches, collect the matched `ActivityAuditResourceType` values. If exactly one value matches, replace the filter with `{ column: 'resourceType', operator: FilterOperatorDto.EQUALS, value: <enum> }`. If zero or multiple values match, pass through the original payload unchanged. For operators other than `contains`, pass through unchanged.
- [x] 2.3 Confirmed during design review: `FilterDto` is single-scalar (`value: string | number`) and `FilterOperatorDto` has no `IN`. Single-match expansion to `eq` is the agreed scope; multi-match support is deferred to a future change.
- [x] 2.4 In `apps/ai-dial-admin/src/constants/grid-columns/grid-columns.tsx`, remove the now-redundant `filterValueGetter` from the `resourceType` column definition (`grid-columns.tsx:251`). The `valueFormatter` and `tooltipValueGetter` stay; server-side filtering is now driven entirely by the request transform.
- [x] 2.5 In `apps/ai-dial-admin/src/components/ActivityAudit/List/List.tsx`, build `resourceTypeLabelMap = useMemo(() => buildResourceTypeLabelMap(t), [t])` and pass it into `getGridFilters(...)` in the existing `getRows` call (`List.tsx:192`).
- [x] 2.6 Add unit tests for the reverse-map helper in `apps/ai-dial-admin/src/components/ActivityAudit/EntityGrid/tests/utils.spec.ts` (or co-located): map shape, locale-dependent rebuild, multiple enum values sharing one label (e.g. `Adapter`/`Application`/`Interceptor`/`Mcp` `ImageDefinition` → `Image`). _(Co-located in `src/constants/grid-columns/tests/formatters.spec.ts`.)_
- [x] 2.7 Add unit tests for the filter transform in `apps/ai-dial-admin/src/components/ActivityAudit/List/tests/utils.spec.tsx` covering each scenario in the spec: single-match `contains` substring expands to `eq` with the corresponding enum, case-insensitive matching, multi-match passes through to original `co` payload, no-match passes through to original `co` payload, `equals` operator passes through, non-`resourceType` filters untouched.

## 3. Audit list preselect consumption

- [x] 3.1 In `apps/ai-dial-admin/src/components/ActivityAudit/List/List.tsx`, add a `useState` lazy initializer that calls `readAuditListPreselect()` (no clearing) and captures the value once at mount. Use it to derive the initial `activityViewType`: `preselect === 'global-firewall' ? ActivityAuditView.Deployments : (viewMode ?? ActivityAuditView.Config)`.
- [x] 3.2 Add a `useRef<boolean>(false)` flag (`hasAppliedPreselectRef`) and an effect that fires when `gridApi` becomes available: if the captured preselect was `'global-firewall'` and the flag is false, call `gridApi.setFilterModel({ resourceType: { type: 'contains', filter: getFormattedResourceType(ActivityAuditResourceType.IMAGE_BUILD_DOMAIN_WHITELIST, t) } })`, set the flag to true, **and then** call `clearAuditListPreselect()`. The clear-after-apply ordering is what makes the flow StrictMode-safe.
- [x] 3.3 Ensure the new effect runs after the existing view-change effect at `List.tsx:248-256` (so the storageKey is already `activity-audit:deployments` when AG Grid persists the filter). _Initial mount path: `activityViewType` is initialized directly to Deployments via lazy initializer, so the view-change effect's `setFilterModel(null)` does not fire (effectiveViewType is unchanged), leaving the preselect filter as the only filter applied._
- [x] 3.4 Update `apps/ai-dial-admin/src/components/ActivityAudit/List/tests/List.spec.tsx` with new scenarios: preselect = `'global-firewall'` selects Deployments view; preselect is cleared from localStorage after first mount; preselect ignored when entity is provided; absence of preselect leaves Config as default; unknown preselect value is cleared and falls back to default. _(setFilterModel assertion deferred — current grid mock doesn't materialize gridApi; behavior verified via end-to-end view change.)_

## 4. Global Firewall popup footer

- [x] 4.1 In `apps/ai-dial-admin/src/components/Deployments/Modals/GlobalWhitelist.tsx`, import `DialPrimaryButton`, `DialNeutralButton`, `DialGhostButton` from the kit (matching the `Info` ConfirmationPopupVariant config: Cancel = Neutral/Outlined → `DialNeutralButton`; Confirm = Primary/Solid → `DialPrimaryButton`; link = ghost), `IconExternalLink` from `@tabler/icons-react`, `useRouter` from `next/navigation`, plus `AuditListPreselect` + `saveAuditListPreselect` and `ApplicationRoute`.
- [x] 4.2 Add `onViewInActivityAudit` handler: calls `saveAuditListPreselect(AuditListPreselect.GlobalFirewall)`, then `onClose()`, then `router.push(ApplicationRoute.ActivityAudit)`. Same-tab client-side navigation via Next's `useRouter` — the popup closes as part of the navigation and the audit page consumes the preselect on mount.
- [x] 4.3 Pass a custom `footer` prop to `DialConfirmationPopup`: a `<div className="flex flex-row items-center justify-between px-6 py-4">` containing the link (`DialGhostButton` with `IconExternalLink` and label `t(DeploymentsI18nKey.ViewInActivityAudit)`) on the left, and the rebuilt Cancel (`DialNeutralButton`) + Apply (`DialPrimaryButton`, or Close in read-only mode) buttons on the right inside a `<div className="flex flex-row items-center gap-2">`. Mirror the existing button labels / disabled flags / loading flags so behavior is identical to the kit's default footer.
- [x] 4.4 Verified the kit's loading branch: the default footer is `null` while `isLoading` is true (`dial-ui-kit.es.js:49788`, `w = r ? null : <div>...</div>`), and a custom `footer` slot is rendered unconditionally (`footer: C ?? w`). The rebuilt footer hides the Cancel/Apply buttons when `isLoading` to match — the link itself stays visible per spec.
- [x] 4.5 Add or extend component tests in `apps/ai-dial-admin/src/components/Deployments/Modals/tests/GlobalWhitelist.spec.tsx` (create if absent): link renders; Cancel/Apply use the correct variant (neutral / primary); click writes the preselect, closes the popup, and navigates via `router.push(ApplicationRoute.ActivityAudit)`; localStorage holds the value at the moment `router.push` is called; `window.open` is NOT called; link visible in read-only mode; Cancel still works as before.

## 5. i18n

- [x] 5.1 Add `DeploymentsI18nKey.ViewInActivityAudit` to `apps/ai-dial-admin/src/constants/i18n.ts` (next to existing Deployments keys like `GlobalFirewall` / `GlobalWhitelist`).
- [x] 5.2 Add the English string to `apps/ai-dial-admin/src/locales/en.ts` — value: `"View in Activity Audit"`.

## 6. Validation

- [x] 6.1 Run `npm run lint` from repo root and resolve any issues (use `--fix` for autofixable ones).
- [x] 6.2 Run `npm run format:write` from repo root.
- [x] 6.3 Run `npm run test` from `apps/ai-dial-admin/` (required for `@/` alias resolution) and resolve any failures. All new tests SHALL pass; existing tests SHALL remain green. _4832 passed, 9 skipped._
- [x] 6.4 Run `openspec validate add-global-firewall-audit-shortcut --strict` and resolve any reported issues.
