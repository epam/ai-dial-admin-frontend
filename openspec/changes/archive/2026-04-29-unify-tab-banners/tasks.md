## 1. Create EntityBanner component

- [x] 1.1 Create `apps/ai-dial-admin/src/components/Deployments/Common/EntityBanner/EntityBanner.tsx` exporting `EntityBanner` with `Props { variant?: AlertVariant; title?: ReactNode; message: ReactNode; className?: string; children?: ReactNode }`. Render `DialAlert` with `variant ?? AlertVariant.Warning`, the supplied `className`, a `message` containing `<span className="small">` wrapping (when `title` is truthy) `<span className="small-text-semi">{title}</span>` followed by the `message` prop value, and pass `children` through. Do not import from `@/src/app/actions/*`, `@/src/context/*`, or `@/src/hooks/*`. Do not call `useEffect`.
- [x] 1.2 Add `apps/ai-dial-admin/src/components/Deployments/Common/EntityBanner/tests/EntityBanner.spec.tsx` covering: default variant is Warning; custom variant forwarded; title rendered as `small-text-semi` when provided; title omitted when undefined; message always rendered; CTA children rendered inside `DialAlert`; `className` forwarded.

## 2. Create ImageStatusBanner wrapper

- [x] 2.1 Create `apps/ai-dial-admin/src/components/Deployments/Common/ImageStatusBanner/ImageStatusBanner.tsx` exporting `ImageStatusBanner` with `Props { image?: Image }`. Move all install-modal state, `installImage` server-action wiring, `useIsReadOnlyAdmin` gating, success-redirect (`router.push(getUrnForEntity(ApplicationRoute.Images, { id: image.id }))`), error-notification flow, and the `createPortal(<ImageInstall .../>, document.body)` block from `Containers/View/TabsContent.tsx` into this wrapper. Render `null` when `!image || !isImageNotInstalled(image)`. Otherwise render `<EntityBanner className="mb-8" message={t(image.buildStatus === IMAGE_STATUS.BUILD_FAILED ? ContainersI18nKey.ImageBuildFailedWarning : ContainersI18nKey.ImageNotInstalledWarning, { imageName: image.name ?? '', imageVersion: image.version })}>` with a `DialNeutralButton` child labelled `t(ContainersI18nKey.InstallImage)` (icon `IconBlocks` size 12) gated on `!isReadOnlyAdmin`. Do not pass a `title` prop. Do not import `DialAlert`.
- [x] 2.2 Add `apps/ai-dial-admin/src/components/Deployments/Common/ImageStatusBanner/tests/ImageStatusBanner.spec.tsx` covering: renders nothing when `image` is undefined; renders nothing when `buildStatus === BUILT`; renders nothing when `buildStatus === BUILDING`; renders `EntityBanner` with `Warning` variant for `NOT_BUILT`; renders `EntityBanner` with `BUILD_FAILED` copy when `buildStatus === BUILD_FAILED`; Install button hidden when `useIsReadOnlyAdmin()` returns true; Install button visible when read-only is false; clicking Install opens the `ImageInstall` confirmation modal. Reuse mocks from `apps/ai-dial-admin/test-setup.tsx`; do not introduce new global mocks.

## 3. Refactor ContainerStatusBanner to delegate rendering

- [x] 3.1 Update `apps/ai-dial-admin/src/components/Deployments/Common/ContainerStatusBanner/ContainerStatusBanner.tsx`: remove the `DialAlert` and `AlertVariant` imports from `@epam/ai-dial-ui-kit`; import `EntityBanner` from `@/src/components/Deployments/Common/EntityBanner/EntityBanner`. Replace the `<DialAlert ...>` render with `<EntityBanner className="mb-6" title={t(ContainersI18nKey.ContainerNotRunningTitle, { type, typeLower: type.toLowerCase() })} message={t(ContainersI18nKey.ContainerNotRunningDescription, { typeLower: type.toLowerCase() })}>` wrapping the existing `DialNeutralButton` (`Go to Container`) child. Keep `view` / `containerId` props, the `getContainersByView(view)` fetch, the `RUNNING` early-return, and the `Container | null` state shape unchanged.
- [x] 3.2 Update `apps/ai-dial-admin/src/components/Deployments/Common/ContainerStatusBanner/tests/ContainerStatusBanner.spec.tsx`: assert against rendered title / description / `Go to Container` button (looked up via accessible role / text) rather than the `DialAlert` wrapper. Keep coverage for: renders null when fetch loading; renders null when container is `RUNNING`; renders null when container not found; renders banner for non-running statuses (`stopped`, `pending`, `crashed`, `not_deployed`, `stopping`); button click invokes `onOpenInNewTab(getContainerRoute(view), { name: containerId })`; correct `getContainersByView` dispatcher resolved per route. Reuse existing mocks from `test-setup.tsx`.

## 4. Move ContainerStatusBanner mount from View.tsx into Properties branch of TabsContent

- [x] 4.1 In `apps/ai-dial-admin/src/components/Applications/View/View.tsx`: remove the `import ContainerStatusBanner from ...` line and the `<ContainerStatusBanner ... />` block (currently rendered between `<SimpleEntityHeader>` and the `flex-1 overflow-auto` div). Keep `View.tsx` otherwise unchanged.
- [x] 4.2 In `apps/ai-dial-admin/src/components/Applications/View/TabsContent.tsx`: import `ContainerStatusBanner`. Inside the `activeTab === EntityViewTab.Properties` branch, render `<ContainerStatusBanner view={ApplicationRoute.Applications} containerId={originalApplication.source.containerId} />` above `<PropertiesTabContent>`, gated on `originalApplication.source?.$type === SOURCE_TYPE.CONTAINER && originalApplication.source?.containerId`. Plumb `originalApplication` (or just the resolved `containerId` and source-type flag) into `TabsContent` props if it isn't already available.
- [x] 4.3 Repeat the View.tsx removal for `apps/ai-dial-admin/src/components/Models/View/View.tsx` (remove import + `<ContainerStatusBanner>` mount). Add the equivalent mount inside `apps/ai-dial-admin/src/components/Models/View/TabsContent.tsx` Properties branch with `view={ApplicationRoute.Models}` and the model's `originalModel.source.containerId`. Plumb `originalModel` into `TabsContent` props if needed.
- [x] 4.4 Repeat for `apps/ai-dial-admin/src/components/Interceptors/View/View.tsx` and `apps/ai-dial-admin/src/components/Interceptors/View/TabsContent.tsx` (use `ApplicationRoute.Interceptors` and the interceptor's `originalInterceptor.source.containerId`). Plumb `originalInterceptor` if needed.
- [x] 4.5 Repeat for `apps/ai-dial-admin/src/components/Toolsets/View/View.tsx` and `apps/ai-dial-admin/src/components/Toolsets/View/TabsContent.tsx` (use `ApplicationRoute.Toolsets` and the toolset's `originalToolset.source.containerId`). Plumb `originalToolset` if needed.
- [x] 4.6 Verify that `View.tsx` for each of the four entity views no longer imports `ContainerStatusBanner` and no longer references `SOURCE_TYPE.CONTAINER`.
- [x] 4.7 Wire Adapters: register `ApplicationRoute.Adapters → getAdapterContainers` in `getContainersByView` (`utils/deployments/containers.ts`), pass `originalAdapter` from `Adapter/View/View.tsx` into `TabsContent`, and add `originalAdapter: DialAdapter` prop + `<ContainerStatusBanner view={ApplicationRoute.Adapters} containerId={originalAdapter.source.containerId} />` mount in the Properties branch of `Adapter/View/TabsContent.tsx`.

## 5. Replace inline image-not-installed banner with ImageStatusBanner

- [x] 5.1 In `apps/ai-dial-admin/src/components/Containers/View/TabsContent.tsx`: remove the inline `DialAlert` block (lines around 118–144), the `DialAlert`/`AlertVariant`/`DialNeutralButton`/`IconBlocks` imports if no longer used elsewhere, the `imageWarning` derived value, the `isInstallModalOpen` state, the `handleInstallModalOpen` / `handleInstallModalClose` callbacks, the `onInstallImage` callback, the `createPortal(<ImageInstall ... />)` block, and the `useIsReadOnlyAdmin` import if no longer used. Keep `image` flowing into `<PropertiesTabContent>` / `<Properties>` as before.
- [x] 5.2 In the same file's `activeTab === EntityViewTab.Properties` branch, render `<ImageStatusBanner image={image} />` above `<PropertiesTabContent>`. Import `ImageStatusBanner` from `@/src/components/Deployments/Common/ImageStatusBanner/ImageStatusBanner`.
- [x] 5.3 Confirm there is no remaining reference in `Containers/View/TabsContent.tsx` to the previous custom CSS overrides (`[&>div]:flex-1`, `[&>div>div:last-child]:w-full`) or to `ContainersI18nKey.ImageNotInstalledWarning` / `ImageBuildFailedWarning` (those keys are now consumed inside `ImageStatusBanner`).

## 6. Update view-level tests for the banner-placement change

- [x] 6.1 Search `apps/ai-dial-admin/src/components/Applications/View/`, `Models/View/`, `Interceptors/View/`, and `Toolsets/View/` for any existing `*.spec.tsx` that asserts on the presence or DOM position of `ContainerStatusBanner` at the `View.tsx` level. Update each affected spec to expect the banner inside the Properties branch of `TabsContent` instead. Reuse existing mocks from `test-setup.tsx`.
- [x] 6.2 Search `apps/ai-dial-admin/src/components/Containers/View/` for any existing `*.spec.tsx` that asserts on the inline image-not-installed `DialAlert`. Update each affected spec to query for `ImageStatusBanner` (or its rendered text/button) in the Properties branch instead.

## 7. Final code quality checks

- [x] 7.1 Run `npm run lint` from the repo root and address any errors / warnings in changed files.
- [x] 7.2 Run `npm run format` from the repo root; if it reports formatting issues, run `npm run format:write` and commit the result.
- [x] 7.3 Run `npm run test` from the repo root and ensure the full suite passes, including the new `EntityBanner.spec.tsx` and `ImageStatusBanner.spec.tsx` plus the updated `ContainerStatusBanner.spec.tsx` and any updated view-level specs.
