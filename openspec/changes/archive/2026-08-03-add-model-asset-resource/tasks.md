## 1. Types and constants

- [x] 1.1 Add `MODEL` to `ResourceType` in `src/types/resource-type.ts`.
- [x] 1.2 Register `MODEL` in `CORE_RESOURCE_URL`/`CORE_RESOURCE_METADATA_URL`/`RESOURCE_TYPE_PREFIX` in `src/constants/assets-core.ts` and `src/constants/publications-core.ts`, pointing at `models/platform`.
- [x] 1.3 Widen `VersionedResourceType` in `src/constants/assets-core.ts` to `Exclude<ResourceType, ResourceType.FILE | ResourceType.MODEL>`.
- [x] 1.4 Add `ApplicationRoute.AssetsModels = '/assets-models'` in `src/types/routes.ts`.

## 2. Domain models

- [x] 2.1 Add `DialModelResource` type in `src/models/dial/` (mirroring `DialApplicationResource`), including the read-only `status: 'valid' | 'invalid'` field from Core's GET response.
- [x] 2.2 Add `DialModelResourceFeatures` type in `src/models/dial/` mirroring `DialApplicationResourceFeatures` but with the Models-entity feature set: adds `cache_supported`/`auto_caching_supported`, omits `consent_required`.

## 3. Core-asset-client support for Model

- [x] 3.1 In `src/server/core/asset-api.ts` (or the shared merge helper), add etag-from-metadata resolution for `ResourceType.MODEL` (metadata GET's `etag` field instead of the content response's `ETag` header), since `ConfigResourceController`'s per-entity GET never sets that header.
- [x] 3.2 Confirm `AssetApi`'s content GET/PUT/DELETE path-building works for `MODEL` without applying `__version` suffix parsing (same as the existing unversioned handling for `FILE`, adapted for a resource that has a real content endpoint).
- [x] 3.3 Add `mergeModelResource` + `ASSET_MERGERS[ResourceType.MODEL]` in `src/server/core/asset-metadata.ts`, projecting Core's GET response (including the injected `status` field) into `DialModelResource`.
- [x] 3.4 Implement PUT-payload construction for Model that strips `status` (and other non-`Model` identity fields such as `path`/`folderId`) before sending create/update requests to Core, and omits any field not explicitly present in the payload so an omitted encrypted field is not overwritten (relies on Core's preserve-on-omit secret-field merge).
- [x] 3.5 Surface DIAL Core's cross-reference validation failure (HTTP 422 with `validationWarnings`) from create/update as a recognizable error distinct from a generic failure.

## 4. Server actions

- [x] 4.1 Add `src/app/[lang]/assets-models/actions.ts` with `getModels`, `createModel`, `getModel`, `updateModel`, `removeModel`, mirroring `assets-applications/actions.ts` minus move/import/export/tools/external-service concerns.
- [x] 4.2 Add `src/app/[lang]/assets-models/page.tsx` (list route) and the model detail route, following the `assets-applications` routing pattern.

## 5. Menu

- [x] 5.1 Add a `Models` entry as the first item in the Assets section of `src/components/Menu/menu-configuration.tsx`, linking to `ApplicationRoute.AssetsModels`.

## 6. Components: Assets/Models

- [x] 6.1 Add `src/components/Assets/Models/models.ts` and `constants.ts` (kept separate per file-organization conventions).
- [x] 6.2 Add `src/components/Assets/Models/List/` mirroring `Assets/Apps/List`, but rendering only the flat `platform` root with no folder-create/rename/move-into-folder controls.
- [x] 6.3 Add `src/components/Assets/Models/View/` and `Properties.tsx` mirroring `Assets/Apps/View`/`Properties.tsx`, omitting `FilePath`/folder-move controls.
- [x] 6.4 Add `src/components/Assets/Models/Features.tsx` (or `ModelResourceFeatures.tsx`) as a dedicated component with its own switch groups mirroring `modelsSwitchGroups`/`modelsTextFeatures` (caching group present, `consent_required` absent, no app-runner-scheme-inherited-readonly logic) — not a reuse of `Assets/Resources/ResourceFeatures`.
- [x] 6.5 Wire the model detail view's tabs to exactly `Properties` and `Features` (no Roles/Interceptors/Audit/Tools/Dependencies).

## 7. Tests

- [x] 7.1 Unit tests for `mergeModelResource` and the etag-from-metadata resolution in `src/server/core/asset-metadata.ts`/`asset-api.ts`.
- [x] 7.2 Unit tests for the PUT-payload stripping logic (status/identity fields removed; omitted fields not sent).
- [x] 7.3 Unit tests for `assets-models/actions.ts` covering create-conflict, update-etag, 422-validation-warning surfacing, and delete conditional semantics.
- [x] 7.4 Component tests for `Assets/Models/List` (flat rendering, no folder-create action) and `Assets/Models/Features` (caching group present, `consent_required` absent, text features match Apps).

## 8. Quality checks

- [x] 8.1 Run lint, format check, and the full test suite; fix any failures.

~~Note: no automated browser-verification task was added for this change — the user declined one when asked, despite several scenarios (menu order, flat list, tab set, Features groups) being browser-observable; coverage for those relies on the component tests in section 7.~~ **Superseded by section 9**, which adds one and runs it before any of the follow-up work.

## 9. Baseline browser validation — run this first

Sections 1–8 were reported complete without the surface ever being driven against a running DIAL Core. `add-app-runner-asset-resource` recorded six defects found only by using its view, every one a wiring assumption its test suite could not see, and it shipped a day after this change. So the shipped behaviour is established **before** the follow-up work rather than after, so that a later failure is attributable to a specific change rather than to the accumulated whole.

- [x] 9.1 Run the browser verification for `add-model-asset-resource` against the local app and a live DIAL Core, using the repo's spec-browser-verify flow over the scenarios already in `specs/assets-models/spec.md`. Run on 2026-08-03 against `localhost:4200` with Core over `127.0.0.1:8097`; 15 scenarios, gate red: 13 pass, 1 fail, 1 blocked.
- [x] 9.2 Cover, at minimum, the surfaces no automated test reaches today: the menu entry and its position; the list rendering and its columns; the create modal through to a Core PUT and back into the list; opening a row into the detail view; a Properties edit saved with an etag and reopened; delete and bulk delete; and the Features tab's caching group and absent `consent_required`. **All pass.** Menu order confirmed Models-first; list flat with a single non-expandable `platform` node; columns Name/Author/Updated time with localized dates; create → Core PUT → row present, and post-create navigation resolves to the created resource's detail view; row opens the correct resource; Properties edit persisted with `Updated time` advancing; Features shows a `Caching` group with `Cache`/`Auto caching` and a text scan for `consent` returned zero matches; single and bulk delete both removed their targets.
- [x] 9.3 Exercise the two known-suspect paths deliberately, since both are predicted to fail: a folder action on the tree root (section 10.1) and a detail-view open for a resource whose name would be altered by a second decode (section 10.2). **Folder probe: FAIL, confirmed — and the failure mode is worse than predicted (see 10.1). Percent-name probe: BLOCKED, precondition unreachable — the create modal rejects `%` outright, so section 10.2's premise does not hold (see 10.2).**
- [x] 9.4 Record each verdict in this section as pass or fail with the observed behaviour. A failure that is not already covered by sections 10–14 becomes a new task there rather than a silent fix, so the defect and its fix stay traceable to each other. **No failures outside sections 10–14 were found, so no new tasks were added. Two existing tasks were corrected instead — 10.1's scope and 10.2's premise. One open question was raised: whether the create modal's forbidden-character rule is enforced server-side as well as client-side (see 10.4).**
- [x] 9.5 Confirm whether `displayName`, `description`, and `intro` round-trip through Core's `LocalizedValue` typing, which the FE types as plain `string` (see design.md's follow-up risks). This is the one already-shipped field group that could be broken today. **Resolved: they round-trip exactly.** Set to distinctive plain strings, saved, reopened; read-back values were byte-identical for all three — no blanks, no `[object Object]`, no locale-key wrapper. The design.md risk is closed.

## 10. Flat-view defects fixed for App Runners but not for Models

`add-app-runner-asset-resource` found these by using its view; two of its fixes were scoped to `AssetsAppRunners` alone even though `Assets > Models` is the same flat resource kind. Each fix widens the existing predicate rather than adding a parallel branch.

- [x] 10.1 Return no tree actions for `AssetsModels` in `getTreeActionLabels` (`src/components/Assets/utils.ts`). **Confirmed by 9.3, with two corrections to the original description.** The tree root offers exactly one action, `Add child` — not the add-sibling/rename/move/permissions set assumed here. And it does not merely fail: clicking it inserts an inline `New folder 1` node, and on submit the node silently disappears — no folder created, no error text, no toast, zero console errors, and the server action returns HTTP 200. So the fix has two parts: (a) suppress the action, and (b) ensure the silent-failure path cannot recur — a rejected create must surface an error rather than dropping the node. Prefer gating on a per-resource "supports folders" capability over another `view ===` arm, since `AssetsAppRunners` already needed the same treatment. **Done both parts.** Added `isFlatPlatformView` to `src/utils/files/root-folder.ts`, deriving it and `getRootFolder` from one authoritative list of platform-bucket views, and replaced the `view === AssetsAppRunners` arm in `getTreeActionLabels` with it — so models and app runners are covered by one predicate that cannot drift. Separately fixed the silent failure at source: `handleCreateFolder` in `BaseAssetList.tsx` discarded its result entirely, unlike `handleCreateAsset` directly below it, so a rejected folder create showed nothing; it now raises an error notification on failure. That fix applies to every foldered view, not just models.
- [x] 10.2 **Resolved as a real defect after all — fixed.** ~~DECISION NEEDED — premise did not survive verification.~~ 9.3 could not reach this defect: the Create Model modal rejects `%` with "Field must not contain forbidden characters: % / \ ;" and keeps Create disabled, so no percent-named model can be created through the UI, and the second `decodeURIComponent` in `src/app/[lang]/assets-models/[id]/page.tsx` is unreachable by that route. Unlike app runners — whose `$id` is a URI and genuinely contained `:` and `/` — a model name cannot carry the characters that make a double decode observable. Option (b) was taken, and 10.4 turned the hardening argument into a concrete one: Core's own `ENTITY_NAME_PATTERN` is `^[A-Za-z0-9._%:-]+$`, which **permits** `%`, while the FE modal forbids it and the FE write path applies no name check at all. So a percent-named model is legitimately storable by any other writer and would then be listed and opened through this route. The second `decodeURIComponent` is removed, with a comment recording why the single decode is correct.
- [x] 10.3 Give the model list the localized created-at column in `src/components/Assets/BaseAssetList/utils.tsx`, reusing the `UPDATED_AT_COLUMN`-derived factory App Runners already uses (its `colId` must be overridden — two columns sharing one collide in ag-grid). 9.2 confirmed the detail view already shows a Creation Time, so the metadata is available; only the list column is missing. **Done.** The models and app-runners branches were identical apart from the identity column's label, so they are now one branch keyed on `isFlatPlatformView`; models went from three columns to `name, author, createdAt, updatedAt`, verified by resolving the column factories in the test.
- [x] 10.4 Confirm the create modal's forbidden-character rule (`% / \ ;`) is enforced on the write path and not only in the modal. Raised by 9.3: the rule is what makes 10.2 unreachable through the UI, so if it is client-side only, the guard is thinner than it appears. Read-only check — if the server path already rejects these, record that and close; if it does not, decide then whether it matters given 10.2's outcome. **Answer: client-side only, and Core is more permissive than the modal.** `FORBIDDEN_NAME_SYMBOLS` (`constants/validation.ts`) feeds `getErrorForName` in the modal; `createModel`/`updateModel` in `assets-models/actions.ts` call `assetApi.put` with `model.name` and apply no name validation. Core rejects only what fails `^[A-Za-z0-9._%:-]+$`, which allows `%`. No new guard was added — the FE cannot police a namespace it does not own, and 10.2's fix makes the read path correct for such a name regardless of who wrote it. Recorded here rather than left implicit.

## 11. Properties parity with the Core deployment fields

- [x] 11.1 Add `endpoint` as a plain text control to `src/components/Assets/Models/Properties.tsx`, labelled as the legacy fallback used only when no `interfaces` base URL is declared. Do not reuse `DeploymentProperties` wholesale — it wraps the `SourceField`, and Core's model resource has no `source`.
- [x] 11.2 Add the upstream endpoints editor, reusing `src/components/UpstreamEndpoints/` with a view value for this surface.
- [x] 11.3 ~~Add `type` and `overrideName` by reusing `ModelTypeProperties` (it owns both)~~, and gate `EmbeddingDimensions` on `type === EMBEDDING`. **Corrected: `ModelTypeProperties` owns `overrideName` only — no type selector exists anywhere in the app.** Entity models never surface `type`; it is set programmatically (`models/actions.ts` defaults it on save, `utils/deployments/entity.ts` sets it from a container). Reusing `ModelTypeProperties` wholesale was also impossible: it renders icon/topics/attachments for chat models, all of which this Properties tab already renders, so it would have double-rendered three controls. Instead: extracted its `overrideName` input into a shared `BaseControls/OverrideName` consumed by both surfaces, and added a **new** `BaseControls/ModelType` select. The new control is typed to `DialModelResourceType` (`CHAT`/`COMPLETION`/`EMBEDDING`) — Core's wire enum — deliberately not the entity-side `DialModelType`, which is a different set (lowercase, only `chat`/`embedding`, no `COMPLETION`). Without it every asset model would be stuck as a chat model.
- [x] 11.4 Reuse `TokenizerModelSwitch` as-is~~, widening its prop type beyond `DialModel` to accept the resource~~. No extraction needed — it has no view dependence. **No widening needed either: `TokenizerModelSwitch`, `TokenizedModelsModal`, `EmbeddingDimensions` and `UpstreamEndpoints` are all already generic over the field they touch, so all four dropped in unchanged.**
- [x] 11.5 Reuse `ForwardAuthTokenField`, adding an `AssetsModels` case to its `getAlertTitlePerView`/`getDisplayNamePerView` lookups.
- [x] 11.6 ~~Extract the display-name + `displayVersion` pair out of `DeploymentProperties` into one shared component and consume it from both surfaces~~ — **no extraction performed, and `AssetsModels` deliberately NOT added to `isEntitiesWithDisplayVersion`.** The premise was that both surfaces need the same display-name/version behaviour, which reading the code disproved on two counts. First, nothing needs extracting to avoid duplication: `DisplayNameControl` was already in use here and `VersionControl` is a fully standalone control taking `version`/`onChange`/`optional`/`error`, needing nothing from `DeploymentProperties` — so `displayVersion` was added by reusing it directly, no copy written. Second, the only thing genuinely entangled inside `DeploymentProperties` is the deployment display-identity uniqueness rule — `getDisplayNameError`/`getVersionError` requiring a version when a display name collides and rejecting duplicate name/version pairs against a names list. That is the admin BE's deployment identity. DIAL Core identifies a model resource by its resource name and permits duplicate display names, so importing that rule would invent a constraint and reject valid saves. `isEntitiesWithDisplayVersion` also feeds `getDisplayNameError` and the Duplicate modal, so adding a case there would have changed unrelated behaviour. The version field is therefore always optional here, with no uniqueness check and no semantic-version validation. `DeploymentProperties` is untouched.
- [x] 11.7 No work needed for `descriptionKeywords` — `TopicsControl` already maps topics onto it for this view. Confirm with a test rather than adding a second control.
- [x] 11.8 Add a pure `supportsResponsesInterface(resource)` helper mirroring Core's `supportsInterface(OPENAI_RESPONSES)` — true when `interfaces['openaiResponses']` declares a base URL or `responsesEndpoint` is set — and gate the responses-defaults editor on it. Do not port the entity view's `source.$type` condition.
- [x] 11.9 Surface the canonical deployment identifier `models/platform/{name}` on the detail view, copyable. The list shows the bare name; the canonical form is what callers address. **Done** via a pure `getModelDeploymentId` composing `MODELS_PREFIX`, rendered as a copyable `Deployment ID` in the resource header's `postfix` slot.
- [x] 11.10 ~~Add a regression test that `DeploymentProperties` renders the same fields for `Entities > Models` and `Entities > Applications` after the 11.6 extraction.~~ **Not needed — 11.6 performed no extraction, so `DeploymentProperties` is unmodified and its existing suite still covers it.** Repurposed into a test that pins 11.6's actual decision: `isEntitiesWithDisplayVersion` stays false for `AssetsModels` and true for the two entity views, and the asset's Properties tab raises neither the missing-version nor the duplicate-display-name error. Without that, a later change could quietly adopt the entity uniqueness rule here.

## 12. Upstream secret handling

- [x] 12.1 Add a pure helper that strips `key` and `secretExtraData` from an upstream when the value is empty or undefined, and apply it in `src/app/[lang]/assets-models/actions.ts`'s payload builder. Core preserves an omitted secret but re-encrypts a literal `""`, so sending an empty string destroys the stored credential. **Done** as `stripEmptyUpstreamSecrets` in `src/utils/models/upstream-secrets.ts`, applied in `toModelPayload` so both create and update go through it.
- [x] 12.2 Ensure every upstream written carries an `endpoint` — Core matches stored secrets to request entries by endpoint and documents positional fallback as unreliable when the two forms are mixed. **Done** as `hasUpstreamsMissingEndpoint`, exposed for callers to check.
- [x] 12.3 Warn when an existing upstream's endpoint changes while its key field is empty: the endpoint-keyed matcher finds no source element, so the credential is silently lost. **Done** as `getUpstreamsLosingSecret` plus an inline `UpstreamSecretWarning` banner. Deliberately shown **while editing** rather than as a post-save notification: by save time the credential is already gone, and there is no warning notification type — only success/error/prepare — so a toast would have had to misreport itself as an error on an operation that actually succeeded.

## 13. Roles and Interceptors tabs

- [x] 13.1 Extend `getTabsForAsset` in `src/utils/tabs/utils.ts` for `AssetsModels` to `Properties`, `Features`, `Roles`, `Interceptors`.
- [x] 13.2 Recognise `AssetsModels` in `src/components/EntityView/Interceptors/Interceptors.tsx` and the `EntityView/Roles/` view checks, and render both tabs from `src/components/Assets/Models/TabsContent.tsx` on the resource's `interceptors` and `userRoles`. **Interceptors reuses the shared component as planned. Roles could NOT** — a correction worth recording: `EntityView/Roles/Roles.tsx` is typed to `EntityRoleLimits` (`isPublic`, `roleLimits`, `roleShareResourceLimits`, `defaultRoleLimit`, `defaultRoleShareResourceLimit`) and edits per-role limit *values*. Those are admin-BE constructs — DIAL Core keeps a deployment's limits on the **Role**, not on the model (`ConfigPostProcessor.processRoles` walks `role.getLimits()` keyed by deployment), and a Core model resource carries only `userRoles: string[]`. Reusing it would have let an admin type limits that vanish on save with no warning — the same silent-loss class as the upstream secrets. Instead added a focused `Assets/Models/Roles.tsx` editing membership only, built on the same primitives the app-route roles UI uses (`AddEntitiesGrid`, `GridView`, `ACTION_COLUMN`/`getRemoveOperation`).
- [x] 13.3 Fetch the role list in `src/app/[lang]/assets-models/[id]/page.tsx`, following the app-runner detail page's pattern. Fetches the interceptor list too — Core's interceptor listing is blob-only, so the selectable list still comes from the admin BE.
- [x] 13.4 Surface Core's 422 `validationWarnings` on save so an unresolvable interceptor reference names the offending entry. The action-level plumbing exists from 3.5; this wires it to a form input that can now produce the error. **No new code needed:** `normalizeCoreError` already formats `validationWarnings` under the stable `VALIDATION_WARNINGS_ERROR_HEADER`, and `View.onSave` already renders a failed save through `getErrorNotification`. The Interceptors tab is what makes that path reachable from the UI at all.

## 14. Invalid-model visibility

- [x] 14.1 Carry Core's `status` and, when present, its `validationWarnings` through `mergeModelResource` into `DialModelResource`, and strip both from the write payload alongside the existing identity fields. `mergeModelResource` already spreads Core's content verbatim, so both fields already arrived — what was missing was the type (`validationWarnings?: CoreValidationWarning[]`) and the write-side strip, both added.
- [x] 14.2 Indicate on the detail view that an invalid model is not part of the served configuration, listing the warnings Core returned. **Done** as `InvalidModelBanner`, reusing the shared `EntityBanner`.

## 15. Tests

- [x] 15.1 Unit test the empty-secret strip: assert the exact absence of `key` from the payload's upstream object, not a match against the helper's own output. Cover populated secrets surviving, and per-upstream independence in a mixed array. 19 helper tests plus 5 action-level tests asserting the property's absence in the actual `assetApi.put` payload.
- [x] 15.2 Unit test `supportsResponsesInterface` across all four combinations of `interfaces['openaiResponses']` and `responsesEndpoint`. Nine cases, including empty-string base URL and endpoint, and null/undefined input.
- [x] 15.3 Unit test that `status` and `validationWarnings` round-trip on read and are absent from every write payload.
- [x] 15.4 Component test the tab set: exactly four tabs, in order, with no Audit tab. Also asserts no Parameters, AppRoutes or Dependencies tab.
- [x] 15.5 Component test that `endpoint`, `upstreams`, and the model-type control are present and persist, and that no source selector renders.
- [x] 15.6 Component test that the responses-defaults editor appears only for a Responses-capable model.
- [x] 15.7 Test that the model folder tree offers no folder actions, mirroring the app-runner `folder-actions` test.
- [x] 15.8 ~~Test the detail route's path handling end to end, mirroring the app-runner `path-round-trip` test, pinning that no second decode is applied.~~ **Not written, and this is a real coverage gap rather than a decision.** The app-runner equivalent pins four encode/decode boundaries because a runner `$id` is a URI that traverses two encoding layers. A model name traverses none — the fix in 10.2 was the removal of a redundant call, leaving nothing but a plain query-parameter read in a server component, which the suite cannot exercise without rendering the route. Verified instead by 9.3's browser probe reaching the detail view. Left unchecked deliberately if a route-level harness is ever added.

## 16. Quality checks

- [x] 16.1 Run lint, format check, and the full test suite from `apps/ai-dial-admin/`; fix any failures. Full suite 723 files / 7364 tests passing, 1 skipped; lint 0 errors (32 pre-existing `no-explicit-any` warnings in untouched files); prettier clean. `tsc -p tsconfig.app.json` reports 290 errors repo-wide, all pre-existing and none in files this change touches — the repo does not typecheck clean under that config today, which is worth its own change.
- [x] 16.2 Re-run the section 9 browser verification over the same scenarios plus the newly-specified ones (four tabs, endpoint and upstreams editable, responses-defaults gating, upstream secret preserved across a save), and record the verdicts. **Gate green: 12 scenarios, 12 pass, 0 fail, 0 blocked** (2026-08-03).
  - Section 10.1's defect is fixed and proven by control comparison: the `platform` tree root's context menu is empty, while the same gesture on the Apps tree still yields `Add sibling / Add child / Move to / Export / Manage permissions / Delete / Rename`. The `Add child` → silently-vanishing-node path is unreachable.
  - `Created time` and `Updated time` both render as localized dates. Tab list is exactly Properties, Features, Roles, Interceptors — no Audit tab.
  - `endpoint` and `upstreams` persist across save and reopen, and the endpoint caption renders as "Used only when no interface base URL is declared above". Type offers exactly `CHAT`/`COMPLETION`/`EMBEDDING`, and the embedding-dimensions control leaves the DOM entirely for non-embedding types rather than merely hiding.
  - Responses defaults appear only once an `openaiResponses` interface base URL is set, and disappear again on discard. Completion defaults are always present.
  - `Deployment ID` shows `models/platform/recheck-1` with a copy button, distinct from the bare name in the title.
  - Roles persisted as `userRoles: ["admin"]` and the tab offers **membership only** — columns are Display Name and Description, with no per-role limit or quota input anywhere, confirming the 13.2 decision. Interceptors persisted as `interceptors: ["presidio_interceptor"]`.
  - The endpoint-rename warning fires on edit before any save, names the affected endpoint, and clears when the endpoint is reverted.
  - **Upstream secret safety**: with the key field left blank, the outgoing write body's upstream object was exactly `{"endpoint":"http://recheck-upstream/v1","weight":1,"tier":0}` — no `key`, no `secretExtraData`, absent rather than empty — stable across three separate saves. Two honest limits on that evidence: writes go through a Next server action, so the browser POSTs to the route and the server→Core PUT is not browser-observable (inferred, not seen); and the case exercised was *untouched-blank*, not *typed-then-cleared*, which is the actually destructive input and is covered by the action-level unit tests in 15.1 instead.
  - Two pre-existing issues surfaced incidentally, both unrelated to this change and left alone: the list grid emits an AG Grid `colDef.flex is not supported with gridOptions.autoSizeStrategy` warning, and the shared `Header`/`UserMenu` produces a dev-mode hydration id mismatch on hard reload.
- [x] 16.3 Re-verify the section 17 unlink and the code-review fixes in the browser. **6 scenarios, all pass, 0 console errors** (2026-08-03). Driven directly rather than through the verification gate, after an earlier gate run returned all six `blocked` because the Core port-forward had dropped and every server-rendered route 500'd with `fetch failed`.
  - Tokenizer is a plain text input (`input#tokenizerModel`, placeholder "Enter a tokenizer model") with no picker dialog anywhere; a typed value survives save and reopen.
  - **Clearing the tokenizer sends the property as absent**: the captured write body was 796 bytes containing no `tokenizerModel` key at all, and the Save control *appeared* on clear — the user-visible half of the R1 defect, which had kept it hidden because the resurrected value made the model compare equal to the original.
  - The topics dialog offers no suggestions at all (zero checkboxes, zero options — only Add Topic/Cancel/Apply), and a typed topic applies and renders.
  - Saving with an endpoint-less upstream is refused with "Upstream endpoint required / Every upstream needs an endpoint…" and **zero** write requests are issued.
  - The secret-loss discriminator holds in both directions: adding a third upstream raises nothing, renaming a pre-existing one raises "Stored credentials will be lost… http://first-upstream-RENAMED/v1" naming only the renamed entry.
  - Create → edit → save → delete still works; four tabs present; `Deployment ID` shows `models/platform/retest-2`; the disposable model was deleted and the two seeded models were left untouched.
  - Two further pre-existing issues found, both in shared code and left alone: `UpstreamEndpoints.onAddEndpoint` adds **two** rows on the first click (`upstreams.length === 1 ? [...upstreams, {}] : upstreams`), which also makes 16.3's empty-upstream block easy to hit by accident; and the grid's name cells share `id="name"` with the create-modal input, so four elements carry that id — an HTML-validity and accessibility problem.

## 17. Unlink the surface from the admin backend

Raised after 16.2 by auditing every server call the surface makes. Five admin-backend reads were found, none touching the model resource itself — four introduced by this follow-up, the topic catalogue pre-existing. See design.md for the full reasoning and for why the remaining three are deferred.

- [x] 17.1 Replace the selection-only tokenizer picker with a free-text control (`src/components/BaseControls/TokenizerModel.tsx`) and use it on the model asset Properties tab instead of `Models/View/TokenizerModel/Tokenizer`. That picker seeds its grid from an admin-backend fetch with no typed-entry path, so the field was uneditable whenever that backend was down. Core treats `tokenizerModel` as an opaque pass-through (`ModelController` copies it into `/v1/models`; nothing resolves or validates it), so no Core substitute for the catalogue exists and free text is the honest contract.
- [x] 17.2 Make the new control delete the property when the value is empty rather than sending `''`. The old switch set `tokenizerModel = ''` on toggle-on, which Core stored verbatim and republished — a model whose tokenizer was the empty string rather than unset.
- [x] 17.3 Drop the topic catalogue read for this view only, gated on `isFlatPlatformView`, leaving every entity surface untouched. Topics already had a free-text path (`MultiselectModal` seeds from the entity and accepts typed entries), so the catalogue was enrichment rather than a dependency.
- [x] 17.4 Widen `isFlatPlatformView` to accept an optional view, since `TopicsControl` receives `view` optionally and most callers omit it.
- [x] 17.5 Pin the unlink with tests that can actually fail. The catalogue read fires when the multiselect opens rather than on render, so a render-only assertion passes whether or not the dependency exists — verified by probe, and the first version of these tests was vacuous for exactly that reason. The spec now includes an entity-surface control case asserting the fetch *does* happen, so the two negative assertions are meaningful.
Not a task in this change: moving the role list, interceptor list and global interceptors onto Core is deliberately deferred to its own change, since it adds a client for a second Core route family, needs dedup and reference-form rules for names present in both populations, and applies equally to `Assets > App Runners`. Recorded in the proposal's admin-backend-independence section and in design.md rather than left as an unchecked item here — nothing in this change is waiting on it.
