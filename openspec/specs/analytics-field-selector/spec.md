# Analytics Field Selector — Spec

## Purpose

Historical capability for the left FieldSelector inside the former Analytics bottom drawer.
Execution Result field visibility/order is now owned by the Display `TreeColumnsPanel`
(`analytics-bottom-drawer`). Run Compare has its own Display panel.

## Requirements

<!-- Requirements removed in redesign-execution-result-bottom-panel — see that change's delta. -->
