# Analytics Comparison Views — Spec

## Purpose

Historical capability for Table/Pivot renderers inside the former Analytics bottom drawer.
Execution Result detail is now specified under `analytics-bottom-drawer` (single-row pivot).
Run Compare Execution Results has its own UI outside this capability.

## Requirements

<!-- Requirements removed in redesign-execution-result-bottom-panel — see that change's delta. -->
